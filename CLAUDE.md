# نظام منح أبانمي — عقد الواجهة

> **اقرا الملف ده قبل ما تكتب أي CSS أو JSX.** القواعد اللي تحت مش
> تفضيلات، هي **مقيسة آليًّا**، والبوّابة بتوقف الكوميت لو رقم طلع —
> هوك `pre-commit` متتبَّع في `.githooks/`. آخر تحديث: **١٤ سبتمبر ٢٠٢٦**.
>
> **أول حاجة على أي نسخة جديدة:** `npm i` (بيوصّل الهوك لوحده) أو
> `npm run hooks:install`. من غيرها الحراسة بالنيّة لا بالكود.

## ما الذي تغيّر في ١٢ سبتمبر (ولازم تعرفه)

جرد شامل (٢٨ مسارًا × ٣ ثيمات) طلّع إن المشكلة مش قرارات تصميم غلط،
هي **غياب مانع**: السلالم إمّا ناقصة وإمّا موجودة ومتجاهَلة. اللي نزل
النهارده هو **الأساس بس** — إضافات بخطر بصري شبه معدوم:

| اتضاف | ليه |
|---|---|
| `--sp-1…11` + أسماء دلالية | **سلّم المسافات مكانش موجود أصلًا** · ٥٤٢ قيمة حرفية بترسم ٥٢ قيمة |
| `--mat-1/2/3` | الزجاج كان بـ١٣ قيمة blur و٨ saturate · مالوش سلّم |
| `--r-hair` · `--r-dot` | بيخرّجوا الـ٩ أنصاف قطر الخارجة من قايمة الخروقات |
| `--fs-1…7` **اتعادت قيمها** | الجسم كان ١٣٫١٢ والثانوي ١١٫٢ · تحت أرضية الـ١٤ المتّفق عليها |
| حذف بلوك تعليق متناقض | كان بيقول `--r1 = 12` وهي ١٦ · وده كان **مصدر باج نشط** |

**وبعدها نزلت تلات جولات كاملة، بالترتيب اللي طلبه العميل:**

| الجولة | القيم الحرفية | المرسوم فعلًا | المفتاح |
|---|---|---|---|
| **الركن** | ٣٨ ← **٠** | ٩ خروقات ← **٠** | `--r-base` |
| **المسافات** | ٨٣٣ ← **٨** | حشو ٣١ ← **٢١** · فجوة ٢٢ ← **٩** | `--sp-base` |
| **الخطّ** | ٣٣٢ ← **٩** | مقاس ٦٧ ← **١٨** · تقدّم ٢٣ ← **٣** | `--fs-base` |

والتلاتة بنفس البنية: **توكن واحد في `:root`، والسلّم كله مشتقّ
منه** — فأي تعديل مستقبلي سطر واحد، وأداة الجرد بتقرا السلّم وقت
التشغيل فما بتحتاجش تحديث.

⚠️ **والبوب أبس داخلة في التلاتة**: `openaudit.mjs` بيفتح ٧٩ طبقة
فعلًا (قايمة · مودال · بانل) ويقيس **العقد الجديدة وحدها** —
الركن ٠ · المسافة ٠ · الخطّ ٠ · ارتفاع السطر ٠.

### جولة الركن (١٢ سبتمبر · بعد الصرف)
**٣٨ قيمة نصف قطر حرفية ← ٠.** الدواير المشروعة اتسمّت `--r-dot`،
والخرقان (`.acctbtn` و`.ejr-ic` كانوا دواير وهمّ تحكّم وصندوق
أيقونة) خرجوا منها · أربعة أرقام شظية (١ · ١٫٥ · ٢ · ٣) بقوا
`--r-hair` · خمسة عناصر كانت كبسولة بالغلط (٤٦–٨٤٪) اتسمّى قصدها ·
وتذييلان ملاصقان لقاع كارت `--r2` كانوا بركن ٠ فبقوا يورثوا ركن
أبوهم. و`--r0` رجع درجة شرعية لنطاق ١٢–٢٨.

### تقليل الركن + المفتاح الواحد (١٢ سبتمبر · طلب العميل)
**الأساس نزل من ٢٤ لـ٢٠** (−٤)، وكل الدرجات نزلت معاه بنفس النسب:
`٣٢←٢٨ · ٢٤←٢٠ · ١٦←١٢ · ١٢←٨ · ٦←٤ · ٢←٢`. والسلّم بقى **مشتقًّا
من `--r-base` وحده** بدل ستّ قيم مكتوبة — فأي تعديل مستقبلي سطر واحد.
والصورة والأفاتار خرجوا من الدايرة لـ`--ric`.

### الشريط الجانبي (١٢ سبتمبر · على كومنت من العميل)
**العرض ٨٠ ← ٦٠ · العلامة ٤٤ ← ٣٢.** ٨٠ كان مقاس ماتيريال ٣، وهو
صحيح لشريط **فيه تسمية تحت كل أيقونة** — وشريطنا أيقونات بلا
تسميات، فكنّا بندفع عرضًا لتخطيط مش مستعملينه. و٦٠ = بند ٤٤ + حشو
٨×٢، يعني الحدّ الهندسي بسلاك صفر. `SHUT` في `Rail.tsx` و`LOCK_AT`
اتعادوا حسابهم مع المقاس الجديد.

### جولة المسافات (١٢ سبتمبر)
**٧٦٧ تصريح حرفي في الـCSS ← ٠ · ٦٦ ستايل سطري في الـJSX ← ٠.**
القيم المرسومة: الحشو من ٣١ ← **٢١**، الفجوة من ٢٢ ← **٩**.

> ### 🔑 مفتاح واحد: `--sp-base`
> ```css
> --sp-base: 4px;   /* ← المفتاح · ١٢ درجة بتتولد منه */
> ```
> ⚠️ **الاشتقاق هنا مضاعفات لا نِسب** (عكس الركن). المسافة **شبكة**:
> لازم تفضل مضاعفًا صحيحًا للوحدة، وإلا الأرقام بتطلع كسورًا وتقع
> بين البكسلات. فالمضاعِفات مقفولة والوحدة وحدها بتتحرّك.

`--sp-1…12` = ٢ · ٤ · ٨ · ١٢ · ١٦ · **٢٠** · ٢٤ · ٣٢ · ٤٠ · ٤٨ · ٦٤ · ٨٠
وفوقهم أسماء دلالية: `--sp-inline` · `--sp-cell` · `--sp-stack` · `--sp-card`.

**استثناءان موثّقان:**
1. **`em` مش `px`** — `gap:.3em` بين الرقم ورمز العملة **مسافة
   طباعية لا شبكية**: لازم تكبر مع الخطّ لا مع التخطيط. الكوديمود
   بيعدّيها عن قصد.
2. **قيمة مشتقّة من مقاس جارها** — زي `padding-top:calc(var(--einf-c)/2)`
   لدايرة بتطلع نصّها فوق اللوحة. دي مربوطة بمصدرها الصحيح
   (الهندسة) لا بالسلّم، والتقريب كان هيكسرها.

### الطبقات العابرة (١٢ سبتمبر · على كومنت من العميل)
التلميح في الشريط الجانبي فضل **بركن ٥٠٪ (كبسولة بالغلط)** بعد جولة
الركن كلها — والعميل هو اللي شافه، مش الجرد. السبب: هو `opacity:0`
لحدّ الهوفر، والجرد بيزور الصفحة ويقيس اللي مرسوم.

`deadcss.mjs` كان قاس المشكلة بالرقم من زمان: **٣٣٥ من ٧٩٦ كلاس
(٤٢٪) ما بترسمش** في المسارات. الرقم كان مكتوب، والدرس ما اتعملش
بيه — لحدّ ما طلع في وش العميل.

**الحلّ: `tools/reveal.mjs`** بيولّد ستايل بيكشف الطبقات العابرة،
**من الـCSS نفسه لا من قايمة مكتوبة**: بيدوّر على كل قاعدة فيها
محفّز (`:hover`/`:focus`/`.on`) وبتخلّي `opacity:1`، وبياخد منها
العنصر اللي بيظهر. فأي طبقة جديدة بتدخل الفحص لوحدها.

```bash
node tools/radaudit.mjs --reveal
```
⚠️ **شغّلها بـ`--reveal` قبل ما تقفل أي جولة شكل.** من غيرها بتفحص
٥٨٪ من السطح وترجع خضرا.

~~**الكنس الكبير لسّه ما نزلش.**~~ **اتشال.** السطر ده كان بيقول إن
الملف فيه ٣٠٦ قيمة خط و٥٤٢ قيمة حشو حرفية — **اتكنسوا كلهم في
الجولات التلاتة تحت**. الباقي في `docs/UI_FIX_PLAN.md` بنود شكل
تانية (الحلقة ٣:١ · الظلال · الكثافة)، مش قيم حرفية.

---

### جولة الخطّ (١٢ سبتمبر)

**٣٣٢ قيمة حرفية ← ٩** (والتسعة كلها `em` أو هندسة · موثّقة تحت).
المقاسات المرسومة فعلًا: **٦٧ ← ١٨**.

> ### 🔑 مفتاح واحد: `--fs-base`
> ```css
> --fs-base: .875rem;   /* ١٤ · الجسم — المفتاح */
> ```
> ⚠️ **والاشتقاق هنا كسور صحيحة لا أعشار.** أول كتابة كانت
> `calc(var(--fs-base) * .857)`، و١٤ × ٠٫٨٥٧ = **١١٫٩٩٨** لا ١٢ —
> فأرضية الـ١٢ وقعت تحت نفسها بجزء من ألف، والجرد رجّع **١٬١٢١
> حالة كاذبة** «نصّ تحت ١٢px». `calc()` بيقسم بدقّة كاملة،
> فـ`* 6 / 7` بيدّي ١٢ بالظبط. اكتب كسورًا.

`--fs-1…9` = ١٢ · ١٣ · ١٤ · ١٦ · **١٨** · ٢٠ · ٢٤ · ٣٢ · **٤٠**
(الدرجتان الجداد اتضافوا بعد القياس: ١١ تصريحًا كانوا محشورين بين
١٦ و٢٠، وخمسة فوق ٣٢ بلا درجة تستقبلهم).

⚠️ **الترقيم اتزحزح:** `--fs-5` القديمة (٢٠) بقت `--fs-6` · و٢٤ بقت
`--fs-7` · و٣٢ بقت `--fs-8`. لو عندك كود قديم بيستعمل الأرقام
دي، راجعه.

### العرض المرن — عيلة تالتة (١٢ سبتمبر)

تمنتاشر صيغة `clamp()` مكتوبة بالإيد بأرقامها الخاصة ← **سبع
درجات** `--fd-1…7`، كل واحدة **طرفاها درجتان من سلّم الخطّ** (١٦→١٨
· ١٨→٢٠ · ٢٠→٢٤ · ٢٤→٣٢ · ٣٢→٤٠ · ٤٠→٦٤ · ٥٦→٨٠). فالمرن بقى
بيتنقّل بين المقاسات الموجودة بدل ما يخترع مقاسات.

⚠️ والوسيط بقى `calc` بأطراف `rem` لا `Nvw` مجرّدة: `3vw` بتربط
المقاس بعرض النافذة وحده، فتكبير المتصفّح ما بيكبّرش النصّ — سقوط
**WCAG 1.4.4**.

### ارتفاع السطر (١٢ سبتمبر)

**١٣٤ تصريحًا حرفيًّا بـ٢٣ نسبة ← ٦ توكنز.** وده أهمّ من مقاس
الخطّ في العربية: **W3C alreq §7.4** — الخطّ العربي محتاج تقدّمًا
رأسيًّا **أكبر** من اللاتيني (الصواعد والنوازل والتشكيل بيطلعوا
برّه الجسم)، والعلاج الصحّ **زيادة التقدّم لا تكبير المقاس**.
عشان كده الأرضية هنا **١٫٦ لا ١٫٥**.

⚠️ **عيلتان لا عيلة واحدة:**

| العيلة | التوكنز | إمتى |
|---|---|---|
| **نِسب** (بلا وحدة) | `--lh-0` ٠ · `--lh-flat` ١ · `--lh-head` ١٫٢٥ · `--lh-ui` ١٫٥ · `--lh-text` ١٫٦ · `--lh-long` ١٫٨ | أي نصّ متدفّق |
| **بكسل** | `--lh-md` ٢٠px · `--lh-xs` ١٦px | تحكّم بسطر واحد · **جزء من حساب `--h-md`** (١٢+٢٠+١٢=٤٤) |

لو حوّلت العيلة التانية لنسب، `--h-md` بيتكسر. وما تستعملش نسبة
في مكان بيحسب ارتفاعًا.

⚠️ **ولا درجة في عيلة النِّسب تحت الأرضية.** `--lh-ui` اتكتبت
١٫٤٥ الأوّل باعتبارها «لتحكّم بسطر واحد» — والقياس لقى خمس حالات
النصّ فيها **لفّ فعلًا** وهي عليها. التوكن اللي قيمته تحت الأرضية
**فخّ**: مفيش بوّابة تقدر تمنع حدّ يحطّه على فقرة. فبقت ١٫٥ بالظبط —
الخرق اتشال من الجذر لا اتصلّح في خمس مواضع.

### فخّان اتمسكوا بالقياس، وكانوا هيعدّوا

**١ · `<small>` بلا قاعدة بيقع على ستايل المتصفّح** (`smaller` ≈
٠٫٨٣em): جوّه نصّ ١٣px بيترسم **١٠٫٨** وجوّه ١٢px بيترسم **١٠** —
تحت الأرضية في كل الثيمات. والجرد ما شافهاش لأنه كان بيقيس مقاس
الخطّ على العناصر اللي فيها **نصّ مباشر**، والـ`small` دي بتلفّ
`<span>` فمالهاش عقدة نصّ · اللي بان كان الرمز جوّاها (١١٫٢px):
**العرَض اتمسك والسبب فلت**. اتحطّت أرضية `small{font-size:var(--fs-1)}`،
والأداة اتعدّلت تمسك اللي **بيحدّد** مقاسًا تحت الأرضية لا اللي
بيرثه.

**٢ · عدّ السطور من الصندوق الخارجي** بدل صناديق السطور: أول كتابة
لـ`leading.mjs` قسمت `getBoundingClientRect().height` على ارتفاع
السطر، فالزرار ٤٤px بسطر ٢٠px طلع «٢٫٢ سطر» وهو سطر واحد جوّه حشو
١٢+١٢ — **تمانية خروقات كاذبة**، وكلها كانت هتتصلّح بتغيير ثابت
الارتفاع بلا داعٍ. العدّ الصحّ من `Range.getClientRects()` مع تجميع
على محور رأسي بسماح نصف سطر (وإلا أيقونة سطرية بتتعدّ سطرًا).

### السطح العايم · الطبقة التالتة (١٢ سبتمبر · على كومنت من العميل)

العميل شاف إن **أي طبقة بتفتح فوق المحتوى في الغامق شفّافة واللي
تحتها بيتقرا من خلالها**. والسبب نفس النمط اللي الملف ده كله
موجود عشانه: **دور بلا سطح خاص بيه**.

السيستم فيه تلات أسطح والفرق بينهم **وظيفي لا جمالي**:

| السطح | التوكن | بيعمل إيه |
|---|---|---|
| الصفحة | `--mesh-bg` | الأرضية |
| الكارت | `--g-card` | **بيرتفع** عن الصفحة · شفّاف بقصد |
| العايم | `--g-pop` | **بيغطّي** المحتوى · معتم بقصد |

والتالت كان ناقصًا، فالطبقات العايمة استعارت وصفة الكارت:
`rgba(var(--wC), N * var(--wM))`. وده صحّ للكارت (في الغامق
`--wM` = ٠٫٠٨٥ عشان اللمسة تفضل خفيفة)، و**كارثة للعايم**: نفس
السطر بيدّي في الفاتح ٩٢٪ أبيض وفي الغامق **٧–٨٪**.

الحلّ: العايم له **لون أساس خاص بكل ثيم** (`--popC`) بألفا
**ثابتة** (`--pop-a` = ٠٫٩٢). فالزجاج فضل زجاجًا والتغطية بقت
مضمونة في الثيمات التلاتة.

**القياس (نفس القايمة · نفس الصفحة):**

| | قبل | بعد |
|---|---|---|
| فاتح | ٣٫٣٪ | ٣٫٣٪ |
| غامق | **٣٢٫٧٪** | **٣٫٢٪** |

(النسبة = متوسّط انحراف بكسلات الطبقة لمّا المحتوى ورَاها يتشال.
الـ٣٫٣٪ الباقية إزاحة `backdrop-filter` نفسها لا تسريب.)

```bash
node tools/overlay.mjs        # عيّنة · أقل من دقيقة
node tools/overlay.mjs --all  # كل المسارات
```

**والقاعدة:** أي حاجة **بتغطّي** المحتوى بتاخد `--g-pop` · وأي
حاجة **بترتفع** عنه بتاخد `--g-card`. الوصفة الحرفية ممنوعة على
الاتنين.

### الشارة اللي جوّاها أيقونة (١٢ سبتمبر · على كومنت من العميل)

`.badge` دور واحد بحشوتين: **حرف** (أوائل اسم جهة) أو **أيقونة**.
الحرف محتاج صندوقًا — هو نصّ، والصندوق هو اللي بيخلّيه علامة لا
كلمة. الأيقونة **علامة بذاتها**، والصندوق حواليها بيضيف حافة
تانية جوّه كارت له حافته: إطارين متداخلين على بعد ٨px.

فالتمييز بالحشوة لا بكلاس جديد:
```css
.badge:has(svg),.badge:has(.aispark){--bdg-ic:82%;background:none;box-shadow:none}
```
أي مكان جديد يحطّ أيقونة في شارة بيدخل القاعدة لوحده — من غير
قايمة كلاسات تقدم وتُنسى. والأيقونة كبرت من ٦٤٪ لـ٨٢٪: اللي كان
بيدّيها ثقلًا هو الصندوق.

### كارت الصرف = كارت السيستم (١٢ سبتمبر · على كومنت من العميل)

**١ · السطح.** `.payq` كان **بيعيد كتابة وصفة الكارت بإيده**
(`--g-card` + الطمس + الحلقة) بدل ما ياخد `.glass` زي `.pcard`
و`.ecard`. الناتج المحسوب كان مطابقًا — والمشكلة بكرة: نسختان
لنفس التعريف بتفترقا أول ما واحدة تتعدّل. بقى `.payq glass`،
والقاعدة في الـCSS **تخطيط وحده**.

> أي كارت جديد بياخد `.glass`. لو محتاج سطحًا مختلفًا، قول —
> ما تنسخش الوصفة.

**٢ · الشريط الجانبي اتشال · وممنوع يرجع.**
`.payq.hold` كان بيرسم `inset 3px 0 0 0 var(--warn)` — شريط
كهرماني على الحافة اليسرى. والحجّة اللي كانت مكتوبة جنبه («اللون
في الأرضية بيخلّي نصّ الكارت كله يتقري تحذيرًا») صحيحة، بس
الخلاصة منها غلط: البديل الصحّ مش شريط على الحافة.

**الحالة معلومة، والمعلومة تتقال بالكلام لا تترسم كحدّ.** والكارت
كان بيقولها **تلات مرّات** أصلًا — وسم «متعثّر · ١٤ يومًا» · صفوف
الفحص بعلامة ⚠ · وسطر الملاحظة. فالشريط كان نسخة رابعة، مرسومة
كحدّ.

ولا كارت تاني في النظام بيعمل كده: `.pcard.sel` بتاخد **حلقة
كاملة** لا شريطًا جانبيًّا.

والبوّابة بقى فيها عدّاد `edgeBars` بيعدّ أي `inset` بعرض أكبر من
١px على جهة واحدة · السقف **صفر**. الحلقة ١px مسموحة، واللي فوقها
على جهة واحدة خرق بيوقف الكوميت.

### عيلة التحكّم · مفتاح واحد (١٣ سبتمبر · طلب العميل)

الزرار والحقل والمنسدلة والشريحة **دور واحد**: حاجة بتتضغط. فالمفروض
سطح واحد وحدّ واحد، **ويتغيّروا من مكان واحد**.

القياس قبل التوحيد (`tools/ctlaudit.mjs`): **خمس تركيبات مختلفة**
لعشر كمبوننتس في كل ثيم — أربعة على العيلة، و`.lbox` على ملء ٥٥٪
وحلقة `--edge-2` (قريّبة لا مطابقة)، وأربعة بلا ملء ولا حلقة، وهوفر
`.fsel-b` كاتب أرقامه بإيده.

**بعد التوحيد: تركيبة واحدة لكل نوع، في الثيمات التلاتة.**

> ### 🔑 مفتاحان: `--ctl-edge` و`--ctl-fill`
> ```css
> --ctl-edge: var(--edge-hair);   /* قوّة الحدّ */
> --ctl-fill: .62;                /* قوّة الملء */
> ```
> كل حالات التحكّم مشتقّة منهم: `--ctl-bg` · `--ctl-bg-h` ·
> `--ctl-bg-a` · `--ctl-line` · `--ctl-line-h` · `--ctl-line-f` ·
> `--ctl-quiet-h` · `--ctl-tone-line`.

⚠️ **والحدّ اتفصل عن الزخرفة.** `--edge-hair` بتحكم حواف الكروت
والفواصل · `--ctl-edge` بتحكم **التحكّم التفاعلي** وحده. الاتنين على
نفس القيمة النهارده فمفيش فرق بصري — لكن رفع الحدّ لـ٣:١ بقى
**سطرًا واحدًا** ما بيلمسش الزخرفة. مقيس:

| `--ctl-edge` | فاتح | غامق |
|---|---|---|
| الافتراضي (٠٫١٥ / ٠٫١٢) | ١٫٢٩–١٫٣١ | ١٫٥٣–١٫٦٤ |
| **٠٫٥٨** | **٣٫١٩–٣٫٢٥ ✅** | **٤٫٨٥–٤٫٩٢ ✅** |

(الفرق بين الكمبوننتس أقل من ٠٫٠٧ في الحالتين — يعني بيتحرّكوا
كتلة واحدة.)

**تلات أنواع مقصودة، والفرق بينها وظيفي:**

| النوع | مين | الشكل |
|---|---|---|
| **محايد** | `.srch` `.fsel-b` `.fchip` `.psize-b` `.lbox` `.chip` | ملء + حلقة |
| **هادئ** | `.iact` `.aclose` `.acctbtn` `.btn-ghost` | بلا إطار وهو ساكن · ملء الهوفر من نفس المفتاح |
| **بنبرة** | `.btn-2` | ملء بلون العلامة · **حلقته من نفس المفتاح** |

> ⚠️ **ما تكتبش ملء ولا حلقة لتحكّم جديد.** خد النوع اللي يناسبه.
> لو محتاج نوعًا رابعًا، قول — ما تخترعش وصفة.

```bash
node tools/ctlaudit.mjs     # لازم ترجع «تركيبة واحدة لكل نوع»
```

### سلّم الملء (١٣ سبتمبر)

**٨٥ تصريحًا بـ٣٧ قيمة ← ٨ درجات.** كل سطح في السيستم
`rgba(var(--wC), calc(N * var(--wM)))`، والمعامل مفتاح شغّال —
لكن الـN كانت بلا سلّم، وفيها تكرار حرفي صريح: `.4` و`.40` ·
`.6` و`.60` · `.8` و`.80` (نفس القيمة بهجاءين، يعني مفيش مرجع).

`--fl-0…7` = ٠ · ٠٫٢٢ · ٠٫٤٢ · ٠٫٥٥ · ٠٫٦٢ · ٠٫٧٤ · ٠٫٨٤ · ٠٫٩٣

الدرجات على **تجمّعات القيم الموجودة فعلًا**، فأكبر إزاحة ٠٫٠٨
والغالبية ≤ ٠٫٠٥ — وده غير محسوس: فرق ٠٫٠٧ في الفاتح = **٤ درجات
من ٢٥٥**، وفي الغامق بيتقسم على اتناشر.

⚠️ **والمفتاح مش جديد: `--wM` هي مفتاح قوّة الأسطح.** اللي كان
ناقص الدرجات. و`--ctl-fill` اتشالت والتحكّم بقى بياخد من السلّم —
مفتاحان لنفس الحاجة أسوأ من واحد.

**درس من غلطة وقعت هنا:** الكوديمود حوّل تعريف السلّم نفسه
(`--fl-4:rgba(...)`) لـ`--fl-4:var(--fl-4)` — تعريف دائري.
والـCSS ما بيقعش على الدائرية، **بيرمي المتغيّر بصمت**: كل
الأسطح بقت شفّافة والبِناء عدّى أخضر. أخطر نوع غلط — صامت
وبيعدّي البوّابة. أي كوديمود بيلمس توكن **لازم يستثني سطر
تعريفه**.

### سلّم الارتفاع والزجاج (١٣ سبتمبر) — **آخر عيلة**

| | قبل | بعد |
|---|---|---|
| وصفة ظلّ خارجي | **٢٤** | **٠** حرفية · ٤ درجات |
| قيمة طمس | **١٢** | **٣** (ديكور مقصود) |
| قيمة تشبّع | **٨** | **٤** |

**الظلّ لغة لا زخرفة** — بيقول للعين قدّ إيه العنصر مرفوع. أربع
درجات كفاية لأي نظام (ماتيريال ٥ · كاربون ٦)، و٢٤ وصفة معناها إن
اللغة مش مفهومة: `6px 16px -8px` هنا و`8px 18px -10px` هناك
و`8px 18px -12px` في التالت — تلات أرقام لنفس الإحساس.

```
--el-1  رفعة خفيفة · هوفر
--el-2  عايم صغير · تلميح · توست
--el-3  عايم · منسدلة · دوك
--el-4  نافذة · مودال · معاينة
```

⚠️ **ولون الظلّ لكل ثيم** (`--el-c`). كان `rgba(20,69,71,…)`
مكتوبًا بالإيد — أخضر داكن. صحّ على خلفية فاتحة، وغلط على غامقة:
ظلّ ملوّن على أسود ما بيبانش.

**والزجاج:** `--mat-1/2/3` كانت موجودة **من أول جولة ومتجاهَلة**،
وجنبها ١٤ تصريح حرفي. دلوقتي كلهم عليها. واتشال الطمس من
**التحكّمات** خالص: سطحها بقى معتمًا، والطمس ورا سطح معتم شغل GPU
مهدور بلا أثر مرئي.

**استثناءات مقصودة:** الحلقة (`0 0 0 Npx`) مش ارتفاع · والإزاحة
الأفقية (درز بين قطاعات) اتجاهية · و`blur(1.5px)` على السكريم
ديكور.

عدّاد `shadowLiterals` سقفه صفر · والعدّاد بيستثني الحلقة
والاتجاهي، وإلا نبضة `0 0 22px 2px` بتتعدّ خرقًا.

### جولة الاتجاه · RTL (١٣ سبتمبر) — **وأداتان كانتا بتكدبا**

التلات بنود اتقفلوا، لكن **الحصيلة الحقيقية أصغر بكتير من المكتوب
في الخطة**: من ٢٨ حالة مبلَّغة، **٧ كانت حقيقية**. الباقي كان
أدوات بتقيس الحاجة الغلط.

| البند | مبلَّغ | حقيقي | بعد |
|---|---|---|---|
| `%` برّه العزل | ١٧ | **٢** | ✅ ٠ |
| التاريخ الخام / `.mono` | ~١٧ موضعًا | ١٧ | ✅ `<DateText>` |
| محاذاة ترويسة العمود | ٥ ثم ١٢ ثم ١١ | **٠** | ✅ ٠ من ٥٧ |

**١ · علامات العزل في يونيكود عزلٌ كامل.** `pct()` بتلفّ الرقم
بـU+2066/U+2069، ودي بتعزل من غير أي CSS. `pctprobe` كان بيفحص
`unicode-bidi` وحدها فرجّع ١٧ حالة، وأكتر من نصّهم معزول أصلًا.
الاتنين الحقيقيين: شرط الدفعة في `PaymentsTab` (جملة عربية جوّاها
«إنجاز 50%» خام) · و`<Num>{n}</Num>%` في `PaymentsPage` — **علامة
النسبة كانت برّه الجزيرة**، فموضعها بيتحدّد بجيرانها لا برقمها.

**٢ · التاريخ له مدخل واحد: `<DateText>`.** `isolate()` كانت
موجودة في `@/lib/format` **ومالهاش ولا نداء واحد**. اتحطّت في ٦
مواضع، واتعمل `<DateText>` وبقى المدخل الوحيد لأي تاريخ (١٧
موضعًا) — بيحلّ الخام و`.mono` (اللي عليها `direction:ltr`
فبتقلب التاريخ العربي) في حتّة واحدة.

**٣ · الترويسة والمحاذاة: أربع غلطات قياس في أداة واحدة.**
البند ده كان مكتوب في الخطة من جرد ٢، والقياس أثبت إنه **مش
موجود**. ومسافة الوصول لـ«مش موجود» كانت أربع إعادات كتابة:

| الغلطة | النتيجة الكاذبة |
|---|---|
| قرا `text-align` المصرَّحة | ٦١ عمود «مخالف» · افتراض `th` هو `center` |
| قارن فراغ اليمين بفراغ الشمال | نصّ طويل بيملا الخانة بيتقرا «يسار» وهو ملزوق يمين |
| عدّ **عقد النصّ** بس | الوسم (`.tag`) نصّه بعيد ٢٠px بحشوه · «قيمة center» |
| عدّ **الأبناء** بس | `.th-t` بتملا الترويسة · العمود بيخرج من الفحص |

والصيغة اللي صحّت: **الذرّة = أصغر صندوق مرسوم** (الابن اللي
بيملا أبوه مش ذرّة · انزل جوّاه)، **والمقارنة مع صندوق حشو آخر
عنصر مالي** نزلنا جوّاه لا مع الخانة (زرار الملف حشوه ٨px،
فمصغَّرته ملزوقة في حافة الزرار لا الخانة)، **والمحاذاة = الحافة
اللي الحبر ملزوق فيها** لا مقارنة الفراغين.

وتلات فخاخ صغيرة اتمسكوا في نفس الأداة: مقبض تغيير عرض العمود
(`.thgrip`) عنصر فاضي على الحافة كان بيمسك الاتّحاد · و`tagName`
لعنصر SVG **بحروف صغيرة** فسهم الشجرة في «البند» كان بيسقط ·
والمحتوى اللي **بيملا** الخانة مالوش محاذاة أصلًا (في RTL الفايض
بيخرج من الشمال والحافة اليمنى بتفضل مكانها مهما كانت
`text-align`).

⚠️ **وأخضر من أداة اتعدّلت لسّه مش دليل.** عشان كده فيها ضابط
سالب:
```bash
node tools/thalign.mjs              # لازم ✅ ٠
node tools/thalign.mjs --selftest   # بيقلب ترويسة العمود الرقمي · لازم يمسك ٢٤
```
لو الضابط السالب رجع أخضر، **الفحص أعمى لا السيستم سليم**.

### البوّابة اتوصلت فعلًا (١٤ سبتمبر · قبل باقي الموديولز)

**الجملة اللي فوق كانت مكتوبة من ١٢ سبتمبر وهي مش صحيحة.** الفحص
كشف: `.git/hooks` فاضية · مفيش husky · مفيش سطر في `package.json`.
يعني `npm run audit` كان **يدويًّا بالكامل** طول الوقت — اللي
يفتكر يشغّله بيشغّله. قاعدة مكتوبة بلا آلة بتنفّذها **وصف نيّة**.

دلوقتي في `.githooks/pre-commit` (متتبَّع في الريبو عبر
`core.hooksPath` عشان أي نسخة أو جلسة تانية تشتغل تحت نفس
الحراسة، مش الجهاز اللي اتظبط بس). أربعة فحوص ساكنة ~١٥ ثانية:

```
cssbalance   تعليق ناقص بيوقع البِناء ويخلّي الجرد يشتغل على dist قديم
routecover   شاشة جديدة بلا مسار مفحوص
typecheck · lint
audit        العدّادات · الرقم ينزل أو يثبت وما يطلعش
```

**واتجرّبت بخرق مصطنع**: ملف فيه `padding:'13px'` سطري — البوّابة
رفضت الكوميت (`inlineStyles` ١١٦>١١٥ · `spacingLiteralsJsx` ٢>٠)
والكوميت ما اتعملش. للطوارئ `git commit --no-verify`.

### 🔑 الأداة اللي بتحرس باقي الأدوات: `routecover.mjs`

كل الفحوص المرسومة بتزور `tools/routes.mjs` **وبس**. فموديول جديد
بيتبني من غير ما مساره يتضاف هناك بيخلّي **كل الأدوات ترجع خضرا
وهي ما زارتش شاشاته** — وده أسوأ من فحص بيسقط: ده فحص **بيطمّن
غلط**.

`routecover.mjs` بيقرا `<Route>` من `App.tsx`، بيحلّ `ROUTES.*` من
`routes.ts`، وبيتأكد إن كل شاشة (ما عدا `<Navigate>` و`*`) ليها
مسار مفحوص واحد على الأقل.

> ### القاعدة لأي موديول جديد
> **المسار بيتضاف في `tools/routes.mjs` في نفس الكوميت اللي
> بيضيف الشاشة.** ولو الشاشة فيها `:param`، حطّ **معرّفًا فعلًا
> فيه بيانات** — مش أول معرّف تلاقيه. (المشروع ٢٠٩٤٠ تابّاته
> فاضية، فالجرد فضل بيقيس شاشتين فاضيتين وهو فاكر إنه بيقيسهم ·
> عشان كده في مشروعان وجهتان في القايمة.)

⚠️ **والأداة نفسها فشلت مرّتين قبل ما تشتغل**، ونفس النمط في
الاتنين: رجّعت **١١ شاشة من ٢٠** وقالت «✅ محروسة». المرّة
الأولى تعبيران ما لقطوش الصيغة الوسط، والتانية `[^}]*` بتقف عند
أول `}` — و`` path={`${ROUTES.projects}/:id`} `` جوّاها `}` في
نصّها، فكل مسار فيه `:param` اتفلت. التسعة المفقودين كانوا
**بالظبط** صفحات التفصيل اللي أي موديول جديد بيضيفها.
الحلّ عدّاد أقواس لا تعبير، و`--list` بيطبع كل مسار اتلقط عشان
العدد يتراجع بالعين.

**ولمّا اشتغلت صح مسكت شاشتين خارج الحراسة من أصلها**:
`/assistant/:id` (محادثة محفوظة) و`/account/preferences`.

### تلات إصلاحات من عين العميل (١٣ سبتمبر · بعد جولة الأشخاص)

التلاتة نزلوا **بعد** ما البِناء والتايبتشيك واللينت والجرد رجعوا
خُضر. مش صدفة — دي حدود اللي بيتقاس دلوقتي:

| اللي شافه العميل | السبب | الإصلاح |
|---|---|---|
| حقل المالك أطول من جاره | وش ٢٨ في حشو ١٢ = **٥٢px** لا ٤٤ | الوش بيغيّر الحشو لا الارتفاع · وأداة `ctlheight` |
| سطر اللمحة معلّق على حافة الكارت | مالوش إزاحة بداية | `padding-inline-start:var(--sp-5)` |
| فراغ ٢٨px جنب «عمر قاسم +1» | الوش كان مربوط بـ`values.length === 1` | الوش بيتبع **الاسم المعروض** لا العدد |

وواحدة تالتة اتمسكت وأنا بصلّح التانية: **شريط الخطر في اللمحة
مكانش بيترسم أصلًا** — `-1.05rem` على حشو ١٢ بيطلع `−٥px` فيتقصّ
بـ`overflow:hidden`. زخرفة `::before` فمفيش أداة بتشوفها.

⚠️ **ودرس رابع، أخطرهم: النسخة القديمة من الأداة بترجع نتيجة
كاذبة.** `overlay.mjs` في بيئة البِناء السحابية كانت النسخة
المكسورة (بتعدّ البكسلات المتغيّرة لا مقدار التغيّر)، فرجّعت **٨
طبقات بتسرّب** والحقيقة صفر. الجهاز فيه النسخة المصلَّحة. **قبل
ما تصدّق نتيجة فحص، اتأكد إن الأداة نفسها هي آخر نسخة** —
`md5sum tools/*.mjs` على الاتنين بيقول في نص ثانية.

### الأشخاص · الوش جنب الاسم (١٣ سبتمبر · طلب العميل)

العميل بعت لقطة لفلتر «كل المشرفين»: **خمس صفوف نصّ متطابقة
الشكل**، وأربعة منها بتبدأ بعين. الاختيار من قايمة زي دي مش
نظرة، هو **قراءة كل سطر لآخره ومقارنة**.

والسبب البنيوي إن **الشخص مكانش سجلًّا أصلًا**: `row.owner` سلسلة،
و`e.by` سلسلة، و`OWNERS` مصفوفة سلاسل. فمفيش مكان تتحطّ فيه صورة
ولا حروف أوّلية ولا مسمّى.

> ### 🔑 مصدر واحد: `src/data/people.ts`
> ```ts
> person('عمر قاسم')   // → { name, initial, slug, photo, title }
> ```
> المفتاح فضل **الاسم** عشان ما نلمسش ولا سطر من الداتا الموجودة،
> والسجلّ بيتعلّق بيه.

⚠️ **والصور بتتلقّط من المجلّد لا من قايمة.** `import.meta.glob`
بيقرا `src/assets/people/*.jpg` وقت البِناء، فوش جديد = **رمي
ملف باسم الـslug**. بلا `import` ولا سطر يتحدَّث ولا حدّ يفتكر.
والناقص بيرجع لحروفه الأوّلية لوحده.

**كمبوننت واحد:** `<Person name={…} />` (وش + اسم) · `<Face />`
(وش وحده). ومقاساته من السلّم: ٢٨ افتراضي · ٣٠ · ٣٤ لشريط القرار
وقايمة الحساب · الركن `--ric` في كلهم.

**النطاق (قرار العميل): كل مكان بتختار أو بتسنِد فيه شخص.**
الفلاتر والمنيوهات · الإسناد الجماعي · كارت المشروع والصرف ·
خلية المالك في الجداول والتقارير · شريط القرار وقايمة الحساب ·
سطر «واقف عند» · كارت «الأشخاص» في الجهة.
**وبرّه النطاق بقرار:** محور الجراف · جمل المساعد · التحية ·
سطر السجلّ — دي مش سطور بتتنقر، فالوش فيها زخرفة.

⚠️ **الحروف الأوّلية محايدة عن قصد · بلا لون لكل شخص.** لون مشتقّ
من الاسم = **عيلة ألوان رابعة بلا مفتاح**، وألوان الجراف محجوزة
للفئات والحالة محجوزة للحالة. والحرفان لا حرف: خمسة من أسماء
السجلّ بيبدأوا بعين.

**تلات فخاخ اتمسكوا بالنظر بعد ما البِناء رجع أخضر:**

| اللي بان | الإصلاح |
|---|---|
| صفّ «الكل» اسمه بيبدأ ٢٨px يمين باقي الأسماء (مالوش وش) | `.prs-gap` بيحجز الخانة |
| أيقونة «مستخدمين» + الوش في نفس الترويسة = نصف المعلومة مكرّرة | الوش **بيحلّ محلّ** الأيقونة في خانة بعرض ثابت |
| عمود المالك ٨٨px بقى يقصّ عند الكلمة الأولى | ١٤٨px · الوش بيعوّض الهوية بس الاسم كان بيبقى بلا فايدة |

```bash
node tools/people.mjs              # جرد الوشوش · وبيقول مين لسّه بلا صورة
node tools/people.mjs --selftest   # الضابط السالب · لازم يمسك
```
⚠️ **الأداة بتدوّر على الأسماء في الـDOM لا على الكلاسات.** «كل
`.prs` جوّاه وش» بيرجع أخضر دايمًا — `.prs` هي اللي بتحطّ الوش،
فالفحص بيتحقّق من نفسه. الأداة بتقرا الرَّوستر وتدوّر على كل اسم
فيه، **والقوايم بتتفتح فعلًا** لأن خيارات الفلتر هي المكان الأهم.

### طول السطر (١٣ سبتمبر)

المقياس الطباعي **٤٥–٨٥ حرفًا في السطر**، وفوقه العين بتضيع وهي
راجعة لأول السطر. جرد ٢ قال **١١ موضعًا لحدّ ٢٣٥ حرفًا** — والقياس
المرسوم لقى **واحدًا** من ٢١ فقرة بتلفّ فعلًا (فكرة المشروع · ١١٠
حرفًا × ٣ سطور).

الفرق إن الجرد القديم عدّ **طول النصّ في المصدر**، والخرق مش طول
النصّ: هو طول **السطر المرسوم**. عنوان ١٢٠ حرفًا في سطر واحد مش
خرق أصلًا.

الإصلاح سطر واحد على `.prose` (`max-width:66ch`) — و`ch` بيتحرّك
مع مقاس الخطّ لوحده، فالسقف بيفضل صحّ لو `--fs-base` اتغيّر.

```bash
node tools/linelen.mjs   # ٤٥–٨٥ حرفًا · على اللي بيلفّ ٣ سطور فأكتر
```

### فاحص التعليقات (١٢ سبتمبر)

غلطة واحدة وقعت **أربع مرّات** في جولة واحدة: تعليق عربي جديد
بيتحطّ بعد `*/` من غير `/*`، فبوستسيإس بيقرا النصّ كأنه كود ويقع
البِناء — والأسوأ إن الجرد بعدها بيشتغل على `dist/` قديم ويرجع
**أخضر كاذب**.

```bash
node tools/cssbalance.mjs     # جزء من npm run build دلوقتي
```

---

## القواعد الملزِمة

### ١ · ممنوع الرقم الحرفي

| الخاصية | اكتب | ما تكتبش |
|---|---|---|
| `font-size` | `var(--fs-3)` | `.82rem` · `13px` |
| `font-size` (مرن) | `var(--fd-3)` | `clamp(1.05rem,1.9vw,1.35rem)` |
| `line-height` | `var(--lh-text)` | `1.6` · `1.45` |
| `padding` / `margin` | `var(--sp-cell)` · `var(--sp-4)` | `.62rem` · `11px` |
| `gap` | `var(--sp-3)` | `.55rem` · `7px` |
| `border-radius` | `var(--r1)` | `16px` · `50%` |
| `color` / `background` | `var(--t2)` · `var(--g-card)` | `#2FC7B6` · `rgba(20,69,71,.09)` |
| `backdrop-filter` | `var(--mat-1)` | `blur(22px) saturate(1.25)` |

**والقاعدة دي بتنطبق على الـJSX زي الـCSS بالظبط.** ستايل سطري فيه
رقم مسافة حرفي = خرق، والبوّابة بتعدّه دلوقتي (`spacingLiteralsJsx`).
لو الكمبوننت محتاج مسافة مختلفة حسب مكانه، استعمل معدِّلًا
(`mt-1…4` · `mb-1…4` · `gp-1…4`) — **مش ستايل سطري**. ودور متكرّر
زي الفقرة العربية له كلاس (`.prose`)، لا ستايل سطري متكرّر في خمس
شاشات بأربع نسب مختلفة زي ما كان.

لو محتاج قيمة مش في أي سلّم، **ده مؤشّر إن السلّم ناقص لا إن حالتك
استثناء**. قول وهنضيف درجة، ما تكتبش الرقم.

### ٢ · الركن بيتحدّد بـ**مقاس العنصر**، مش بدوره

الرقم المجرّد ما بيقولش حاجة: ١٦ على صندوق ٣٢ **دايرة**، وعلى كارت
٤٠٠ **شعرة**. فالقاعدة نسبة:

> ### 🔑 مفتاح واحد: `--r-base`
> السلّم كله **مشتقّ بنسب ثابتة** من توكن واحد في `:root`. عايز
> الركن أنعم أو أحدّ في السيستم كله؟ **غيّر `--r-base` وبس** — ولا
> سطر تاني بيتلمس، ولا أداة بتحتاج تحديث (الجرد بيقرا السلّم من
> `:root` وقت التشغيل).
>
> ```css
> --r-base: 20px;   /* ← المفتاح */
> ```

| ضلع العنصر | التوكن | النسبة | القيمة عند ٢٠ |
|---|---|---|---|
| < ١٢px (شظية · خطّ · مؤشّر) | `--r-hair` | ٠٫١× | ٢px |
| ١٢–٢٨px (صندوق أيقونة صغير · خانة تحديد) | `--r0` | ٠٫٢× | ٤px |
| ٢٩–٤٠px (صندوق أيقونة · **صورة** · تحكّم صغير) | `--ric` | ٠٫٤× | ٨px |
| ٤١–٨٠px (زرار · حقل · منسدلة · صفّ) | `--r1` | ٠٫٦× | ١٢px |
| > ٨٠px (كارت · لوح · حاوية) | `--r2` | ١٫٠× | ٢٠px |
| لوح كبير · مودال · شيت | `--r3` | ١٫٤× | ٢٨px |
| **نقطة فقط** | `--r-dot` | — | ٥٠٪ |
| تاب · وسم · شريحة · تراك شريط | `--rp` | — | ٩٩٩px |

**الكبسولة والدايرة مش درجات مقاس، هما دورَان** — فما بينزلوش مع
الأساس ولا بيتأثروا بيه.

**الدايرة للنقطة وحدها.** الصورة والأفاتار **بيتربّعوا** (`--ric`)
عشان يمشوا مع الهندسة بدل ما يشذّوا عنها · وتحكّم تفاعلي دائري = خرق.
`border-radius:50%` مكتوب بالإيد = خرق كمان، حتى لو الشكل صح · اكتب
`var(--r-dot)` عشان القصد يبان.

**والشرط الحاكم نسبة لا نطاق:** الركن ÷ أصغر ضلع لازم يقع بين
**١٢٪ و٤٥٪**. فوق ٤٥٪ الشكل بقى كبسولة بالغلط (لو المقصود كبسولة
اكتب `--rp`)، وتحت ١٢٪ على عنصر أصغر من ٨٠px الركن بيبان حادًّا
غريبًا. فوق ٨٠px النسبة الصغيرة طبيعية (كارت ٨٠٠ بركن ٢٤ = ٣٪).
`node tools/radaudit.mjs` بيفحص ده آليًّا.

**استثناءان موثّقان:** أعمدة الجراف الرأسية · والركن الجزئي على بعض
الجهات (وصلة مقصودة، بيطابق الأب لا بيتناسب مع الجسم).

الاستثناء الأول مكتوب **في الـCSS نفسه** بعلامة `--r-exempt:1` على
القاعدة، لا في قايمة كلاسات جوّه الأداة: القايمة بتقدم وتُنسى،
والعلامة بتمشي مع الكود اللي بتستثنيه وبتموت معاه. محتاج تستثني
حاجة؟ حطّ العلامة واكتب السبب جنبها.

**الركن المتداخل:** القاعدة بتنطبق على **الابن الملاصق للركن بس**
(لو `inset < ركن الأب`)، وساعتها `inner = outer − inset` بالظبط. الابن
اللي في نصّ الكارت ركنه مالوش علاقة بركن الكارت.

### ٣ · الزجاج له حدود، والعميل حدّدها

- ✅ **مسموح:** الكروم (توب بار · ريل) · الطبقات العايمة (مودال · دوك · قايمة) · الترانزيشنز.
- ❌ **ممنوع:** أي سطح جوّاه **رقم قابل للقرار** — جدول · حقل · منسدلة · خلية.

السبب مقيس: حقل البحث حاليًّا بيفصل عن خلفيته بـ**١٫١٥:١**، والزجاج
بيشيل الحدّ **والملء** مع بعض فما بيفضلش حاجة تعرّف الكمبوننت. ده
**سقوط WCAG 1.4.11** ومجدول للإصلاح — ما تزوّدهوش.

### ٤ · أي تحكّم تفاعلي: الحدّ ≥ ٣:١ — **اتطبّق ١٣ سبتمبر**

الحلقة حوالين زرار أو حقل أو منسدلة **تتقاس**، ما تتكتبش ألفا
وتتمنّى. والقياس دلوقتي أخضر:

| | فاتح | غامق |
|---|---|---|
| حقل البحث · المنسدلة · الشريحة · حقل الدخول · زرار المساعد | **٣٫٢١–٣٫٢٦** | **٣٫١٢–٣٫٣٢** |

⚠️ **القيمة لكل ثيم مختلفة عشان الناتج يتساوى.** اللي بيتشاف هو
الفرق بين الحدّ والسطح، والسطح مختلف في كل ثيم — فرقم واحد
(٠٫٥٨) كان هيدّي ٣٫٢ فاتح و**٥٫٢** غامق. القيم: **فاتح ٠٫٥٨ ·
غامق وأخضر ٠٫٤٠**، والجدول الكامل في `:root`.

⚠️ **ومش كل حاجة ليها حدّ لازمها ٣:١.** المعيار بيقول «المعلومة
البصرية **اللازمة لتمييز** عناصر الواجهة» — يعني الشرط على اللي
**حدّه هو الدليل الوحيد** إنه تحكّم. اللي بيتعرّف بمحتواه (كارت
فيه عنوان ورقم · وسم فيه كلمة · تاب شكله أندرلاين · فاصل صفّ)
حدّه زخرفة.

الأداة كانت بتحطّ التسعة في سلّة واحدة فبتبلّغ عن **٧ خروقات**
والحقيقة **٤**. اتقسمت لـ«لازم» و«للعلم» — **فحص بيبلّغ عن
خروقات مش حقيقية بيتعوّد الناس يتجاهلوه.**

```
node tools/nontext.mjs
```

### ٥ · الاتجاه (RTL) — تلات مصايد معروفة

1. **أي رقم أو `%` جوّه جملة عربية يتلفّ.** `<span className="num">{n}%</span>`
   — علامة الـ`%` **جوّه** العزل لا برّاه: `<Num>{n}</Num>%` غلط،
   `<Num>{pct(n)}</Num>` صحّ. ولو الجملة جاية جاهزة (من الداتا أو
   من وثيقة العميل) استعمل `isolate()` من `@/lib/format` — بتلفّ كل
   تسلسل رقمي في جزيرة من غير ما تلمس النصّ.
   ```bash
   node tools/pctprobe.mjs   # لازم ٠
   ```
2. **التاريخ من `<DateText>` وحدها.** مش `readDate()` بالإيد ولا
   نصّ خام: الكمبوننت هو المدخل، وبيحلّ الفخّين مع بعض — الصيغة
   الخام (`2026-04-12`)، و`.mono` اللي عليها `direction:ltr`
   فبتقلب التاريخ العربي بصريًّا. **ما تلفّش تاريخًا بـ`.mono`.**
3. **ترويسة العمود ترث محاذاة قيمه** — والآلية موجودة: `n: true`
   على العمود بتحطّ `.n` على الترويسة **والخانة** مع بعض، و
   `.tbl th.n,.tbl td.n{text-align:left}` بتاخدهم الاتنين. فما
   تكتبش `text-align` لعمود، **علّم العمود رقميًّا**.
   ```bash
   node tools/thalign.mjs              # لازم ٠
   node tools/thalign.mjs --selftest   # الضابط السالب · لازم يمسك ٢٤
   ```

### ٦ · الارتفاع ناتج، مش رقم مكتوب

`--h-md: 44px` = ١٢ + ٢٠ + ١٢. **كل تحكّم تفاعلي في نفس الصفّ ارتفاعه
`--h-md`** بلا استثناء — زرار وحقل وبحث وشريحة وتاب ومبدّل.
«صغير» = مقاس خطّ أصغر (`.btn-sm`)، **مش صندوق أصغر**.

⚠️ **والقاعدة دي كانت مكتوبة بلا أداة، فاتكسرت بصمت (١٣ سبتمبر).**
أول ما دخل وش ٢٨px في ترويسة الفلتر بقى الحساب ١٢ + ٢٨ + ١٢ =
**٥٢px**، والبِناء والتايبتشيك واللينت والجرد كلهم رجعوا خُضر —
والعميل هو اللي شاف إن الحقل أطول من جاره في نفس الصفّ.
الإصلاح: **الوش بيغيّر الحشو لا الارتفاع** —
`.fsel-b:has(.fsel-face){padding-block:var(--sp-3)}` فيرجع
٨ + ٢٨ + ٨ = ٤٤.

**أي قاعدة بلا أداة بتقيسها هي نيّة لا عقد:**
```bash
node tools/ctlheight.mjs              # ١٩٩ تحكّم · كلهم على --h-md
node tools/ctlheight.mjs --selftest   # بيرجّع غلطة الـ٥٢px · لازم يمسكها
```
(الأداة بتقرا `--h-md` من `:root` وقت التشغيل، فتغيير الرقم ما
بيحتاجش تحديث فيها.)

### ٧ · الشخص بيتعرض بوشّه

أي اسم بني آدم في مكان **بتختار أو بتسنِد** فيه بيترسم بـ`<Person>`،
مش بـ`<span>{name}</span>`. والاسم بيتحوّل لسجلّ من `person()` في
`src/data/people.ts` — ما تكتبش صورة ولا حروف أوّلية بإيدك.

- فلتر خياراته أشخاص بياخد `people` (خاصية، **مش استنتاج**: «الرياض»
  و«أحمد» نصّان لا فرق بينهم برّه السياق).
- عمود جدول قيمته شخص بياخد `person: true` (تقارير الشاشة) أو
  `kind: 'person'` (التقارير الحيّة).
- **«بلا مالك» و«غير مُسنَد» و«النظام» مش أشخاص** — القايمة في
  `NOT_A_PERSON`، والقيم دي بتفضل نصًّا أو وسمًا. أفاتار بحروف
  أوّلية على «بلا مالك» بيتقرا كأنه بني آدم اسمه كده.
- وش جديد = **ملف في `src/assets/people/<slug>.jpg`** وسطر في
  الرَّوستر. لا `import` ولا تعديل في أي كمبوننت.

### ٨ · عرض البيانات

- كل خلية رقمية: `font-variant-numeric: tabular-nums` (السيستم ١٠٠٪ نضيف هنا — حافظ عليه).
- ألوان الجراف من `--ch-1…6` · **الألوان التحذيرية (`--ch-warn` · `--ch-late`) محجوزة للحالة**، ما تتصرفش على فئات.
- اللون مش وسيلة وحيدة للترميز (WCAG 1.4.1) — لازم شكل أو نصّ معاه.
- الطول والموضع أدقّ ترميز · المساحة والزاوية أسوأهم. **بلا pie ولا gauge ولا 3D.**
- **الحلقة (donut) مسموحة بشرط واحد: النسبة مكتوبة على القوس.**
  ⚠️ القاعدة دي كانت «بلا donut» و**العميل غيّرها في ١٣ سبتمبر** —
  والشرط جزء من قراره لا تحفّظ عليه. السبب: الاعتراض الأصلي مش على
  الشكل، هو إن القارئ ما بيقدرش يقدّر ٥١٪ من زاوية فبيقراها من
  اللِّيجند — يعني الرسم زخرفة والرقم بعيد عنه. لمّا الرقم يتكتب
  **على القوس**، الاعتراض بيسقط: الرقم عند الشكل اللي بيمثّله.
  وساعتها اللِّيجند بيبقى تسميات وألوان بس في صفّ (`<Legend inline />`).
- **كل لون جراف معاه حبره المقيس** (`--on-ch-1…6`). نصّ فوق لون =
  **٤٫٥:١** (WCAG 1.4.3)، والحبر الواحد ما بيكفيش: الأبيض على
  `--ch-1` الغامق ١٠٫٦٥ وعلى `--ch-3` الفاتح ٢٫٢١ (سقوط). أي لون
  جراف جديد يجي ومعاه حبره · `tools/contrast.mjs` بيقيسها.
- **سُمك شريط الحلقة بيتحدّد بعرض أعرض رقم فيه، لا بالذوق.** الحالة
  الحرجة عند الساعة ٣ و٩ (الشريط رأسي والنصّ أفقي): لازم **عرض
  الرقم < سُمك الشريط**، وإلا الرقم بيطلع برّه القوس على خلفية
  الكارت. عند رقم ٨ وحدات، الشريط ٢٢.
- كل الأرقام بخانات لاتينية (٠–٩ لاتيني) حتى جوّه النصّ العربي · `nf` من `@/lib/format`.

### ٩ · الطباعة: تلات عيلات، وكل واحدة ليها شغلها

| العيلة | التوكن | إمتى |
|---|---|---|
| **ثابتة** | `--fs-1…9` | كل نصّ عادي · مقاس واحد على كل العروض |
| **مرنة** | `--fd-1…7` | الأرقام البطلة والعناوين الكبيرة بس · بتكبر مع النافذة بين درجتين |
| **ارتفاع السطر** | `--lh-*` (نِسب) · `--lh-md/xs` (بكسل) | الأولى للنصّ · التانية للتحكّم |

**والأرضية اتنين رقم:**
- **١٢px** لأي نصّ مرسوم · وده يشمل اللي بيورّث من `em` و`<small>`.
- **١٫٦ نسبة** لأي نصّ **بيلفّ على أكتر من سطر** (١٫٥ الحدّ الأدنى المطلق).

الأرضية التانية **بتتقاس لا بتتفحص من قايمة**: `line-height:1.45` على
زرار سليم تمامًا (سطر واحد · الصندوق هو اللي بيحدّد الارتفاع)، ونفسها
على فقرة **خرق**. الفرق مش في الكلاس ولا في المقاس — في إن النصّ لفّ
ولا لأ، وده ما بيتعرفش غير بالرسم:
```bash
node tools/leading.mjs
```

**استثناءات موثّقة** (خارج السلّم عن قصد، والجرد بيعدّيها):
`em` الطباعي (رمز الريال `.rs` · لواحق `<small>` جوّه رقم بطل) ·
القيمة المشتقّة من هندسة جارها (`--init-box`) · `inherit`.

---

## قبل ما تقفل أي شغل

```bash
npm run typecheck && npm run lint     # الكود
npm run build                          # بيشغّل cssbalance الأول · لازم يعدّي
npm run audit                          # ← البوّابة الثابتة

# ⚠️ وقبل ما تقفل **أي جولة شكل**، الفحوص المرسومة:
node tools/radaudit.mjs --reveal       # الركن · مع الطبقات العابرة
node tools/leading.mjs                 # الأرضية العربية للتقدّم
node tools/openaudit.mjs               # البوب أبس · بيفتحها فعلًا
node tools/vizaudit.mjs                # توزيع كل قيمة مرسومة
node tools/overlay.mjs                 # تعتيم الطبقات العايمة · **الثيمات كلها**
node tools/pctprobe.mjs                # انقلاب الـ% في RTL
node tools/thalign.mjs                 # محاذاة ترويسة العمود
node tools/linelen.mjs                 # طول السطر المرسوم
node tools/people.mjs                  # الوش جنب الاسم
node tools/ctlheight.mjs               # ارتفاع التحكّم
```

⚠️ **البوّابة الثابتة بتمسك الرقم الحرفي وبس.** خرق مكتوب بتوكن
(زي نسبة تحت الأرضية على فقرة) ما بيعدّيش منها — لازم القياس.

`npm run audit` بتقارن بـ`ui-budget.json`. **الرقم ينزل أو يفضل زيه،
وما يطلعش أبدًا.** لو طلع، الأداة بتقول أي عدّاد وفين.

لو نزّلت رقمًا (إصلاح حقيقي)، ثبّته:
```bash
npm run audit:lock
```

---

## خرايط للتوثيق

| الملف | فيه إيه |
|---|---|
| `docs/UI_STANDARDS.md` | المعايير ومن فين بنجيب «الأفضل» |
| `docs/UI_AUDIT_02.md` | الجرد البصري المستقل · كل الأرقام |
| `docs/UI_DIRECTION_02.md` | تقييم الدايركشن · فحص المنحنيات · تعريف «الكمال» كجدول |
| `docs/UI_FIX_PLAN.md` | **٣٠ بند مجدولين** · إيه نزل وإيه مستنّي |
| `src/styles/index.css` | التوكنز في `:root` مع تعليق بيشرح **ليه** كل رقم |

## أدوات الجرد (كلها في `tools/`)

`uiaudit` درِفت الأدوار · `uiaudit-css` القيم الحرفية · `uicheck` الكسر واللقطات ·
`contrast` تباين النص · `nontext` تباين غير النص · `a11y258` مقاس الهدف ·
`vizaudit` توزيع كل قيمة مرسومة · `radaudit` + `radnest` الركن والتراكز ·
`pctprobe` انقلاب الـ`%` · `thalign` محاذاة ترويسة العمود (وفيه `--selftest`) ·
`linelen` طول السطر المرسوم · `people` الوش جنب الاسم ·
`ctlheight` ارتفاع التحكّم (والاتنين فيهم `--selftest`) ·
`own` ملكية الكلاسات · `deadcss` الكلاسات الميّتة ·
`leading` الأرضية العربية للتقدّم · `openaudit` البوب أبس (بيفتحها فعلًا) ·
`reveal` الطبقات العابرة · `cssbalance` توازن التعليقات (قبل البِناء) ·
`overlay` تعتيم الطبقات العايمة (بالتصوير · بيمسك اللي بيبان في ثيم واحد)

والكوديمودات: `radaudit`→ يدوي · `spacemod` + `spacejsx` المسافات ·
`typemod` + `typejsx` مقاس الخطّ · `lhmod` ارتفاع السطر.

محتاجين `npm i -D playwright pngjs` و`npm run build` قبل التشغيل.

---

## نقطة أخيرة، وهي الأهم

الدرس اللي طلع من الجرد: **فحص بيتحقّق من قايمة مكتوبة بيرجع أخضر لو
السلّم نفسه ناقص.** `uiaudit` كان بيقول «انحراف ٠» بينما ٦٨ مقاس خط
بيترسموا فعلًا. فأي أداة جديدة تتكتب هنا لازم **تقيس التوزيع الحقيقي
المرسوم**، وبعدين تقارنه بسلّم — لا العكس.
