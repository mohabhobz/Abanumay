# جرد الواجهة رقم ١

**التاريخ:** ١٠ سبتمبر ٢٠٢٦ · **النسخة المقيسة:** `v1.0-pre-ui-audit`
**النطاق:** ٢٣ مسارًا × ٣ ثيمات = ٦٩ قياس صفحة · ملف ستايل ٣٬٧٣٢ سطرًا
**الأداة:** `tools/uiaudit.mjs` + `tools/uiaudit-css.mjs` — يتشغّلوا تاني في أي وقت

---

## ١ · السبب

نظام الألوان موكَّن بالكامل؛ **الشكل والمقاس والحشو لأ**. كل شاشة جديدة
كانت بتكتب أرقامها بإيدها لأن مفيش سلّم مفروض يمنعها. فالاختلاف مش نتيجة
قرار غلط — هو **النتيجة الحتمية** لغياب مانع. وده معناه إن الإصلاح
بالترقيع بيرجع بعد شهر؛ اللي بيمنع الرجوع هو السلّم + الفحص الآلي.

---

## ٢ · الأرقام

| القياس | الآن | الهدف |
|---|---|---|
| **انحراف الأدوار وقت التشغيل** | **٤٥** | ٠ |
| مقاسات خط في الستايل | ٨٥ | ٧ |
| قيم حشو | ١٥٩ | ٩ |
| قيم `gap` | ٦٢ | ٩ |
| نصف قطر حرفي | ١٠٩ (٧٠ خارج السلّم) | ٠ |
| ألوان حرفية `#hex` | ١٥٣ استعمال · ٧٩ قيمة | ٠ |
| صناديق مستطيلة يفترض إنها مربّعة | ٣ | ٠ |
| ركن متداخل مكسور | ٥ | ٠ |
| حاوية قاصّة بلا حشو | ٢ | ٠ |
| صفحات بنمطَي منسدلة | ٧ | ٠ |
| خلايا رقمية بلا `tabular-nums` | **٠ من ١٬٣٦٨** | ٠ ✅ |
| أكتر من دعوة أساسية في شاشة | **٠** | ٠ ✅ |

---

## ٣ · اللي الأداة لقته والعين ما شافتهوش

### ٣.١ خمس تنفيذات مختلفة للاستِبر 🔴

مش «مرة كيرفي ومرة شارب» — **خمسة كمبوننتس منفصلة بتعمل نفس الحاجة**:

| التنفيذ | المكان | قطر النقطة | التخطيط |
|---|---|---|---|
| `.steps` / `.step` | أفقي عام | **١٠px** | grid أعمدة + خط واصل |
| `.stepv` (`VSteps`) | رأسي | **١٠px** | flex عمود |
| `.agr-step` / `.agr-dot` | الاتفاقية | **١٦px** | grid ٣ أعمدة |
| `.pay-step` / `.pay-dot` | الدفعات | **١٥px** | grid auto-fit |
| `.th-step` / `.th-dot` | تفكير المساعد | **٥px** | flex سطر |

**١٥px مقابل ١٦px** هي بصمة الدرِفت الكلاسيكية: رقمان اتكتبوا بالإيد
في وقتين مختلفين لنفس الشيء. **الإصلاح:** كمبوننت `<Steps>` واحد
بمحورين (أفقي/رأسي) وقطر نقطة من السلّم.

### ٣.٢ الحقل كبسولة في مكان ومستطيل في مكان 🔴

| الشاشة | نصف القطر | الارتفاع |
|---|---|---|
| `/projects` · `/entities` | **٩٩٩px** (كبسولة) | ٢٨ |
| `/budget` · `/reports` | **١٠px** (مستطيل) | ٢٩ |

نفس الدور، شكلان مختلفان تمامًا. ده أوضح من كل اللي في قايمتك ومحدش
ذكره.

### ٣.٣ شرائح الحالة اتكتبت مرتين

`.fsegs` في `/projects` و`/entities` و`/reports` جايّة من كمبوننت
`Segments` وارتفاعها **٣١px**. في `/budget` نفس الشكل **مكتوب ماركب
بالإيد** وارتفاعه **٣٠px**. البكسل مش المشكلة — المشكلة إن فيه نسختين
من نفس الكمبوننت، والتانية مش هتاخد أي تحسين يحصل في الأولى.

### ٣.٤ الوسم بأربعة ارتفاعات

١٥ · ١٨ · ١٩ · ٢٠px. الأغلبية الساحقة ١٨ (٣٠ موضع)، والباقي حالات
فردية في `/` و`/budget`.

### ٣.٥ النصّ المساعد بخمستاشر مقاس

`.sub` مقاسه بيتراوح من ١٠.٨٨ لـ١٢.٨px عبر الشاشات. دور واحد،
خمستاشر قيمة.

### ٣.٦ ألوان التوكنز مكتوبة حرفيًا كمان

`#2FC7B6` (٦ مرات) · `#144547` (٤) · `#A8D93C` (٤) · `#E8B045` (٤) —
دي **كلها توكنز موجودة**، ومكتوبة تاني بالإيد. يعني تغيير اللون في
`:root` مش هيوصلهم.

### ٣.٧ ملاحظة على الوثيقة نفسها ⚠️

سلّم الوثيقة حطّ الوسم في `--h-sm: 28px`. **القياس بيقول ٱلوثيقة غلط**:
الوسم الحقيقي ١٨px بخط ١٠.٨٨، وده الصح لجدول كثيف — ٢٨px هيبقى ضخمًا
داخل السطر. الوسم **مش عنصر تحكّم**، هو تسمية غير قابلة للضغط، فمالوش
دعوة بسلّم الأهداف اللمسية.

**التعديل المطلوب على الوثيقة:** إضافة `--h-xs: 20px` للتسميات غير
التفاعلية، والوسم ياخده.

---

## ٤ · ملاحظاتك الإحدى عشرة — الحالة بعد القياس

| # | الملاحظة | الحالة | التفصيل |
|---|---|---|---|
| ١ | أيقونات المنيو مستطيلة | ✅ **مؤكَّدة** | `.vtog button` = **٣٢×٣٠** في `/entities` |
| ٢ | اللوجو المصغّر/الممتد | ⚪ بصري | مش قابل للقياس — قرار في الوثيقة |
| ٣ | CTA مش موحّدة | ❌ **مردودة** | الخمس فاريَنتس **لونها ثابت** في التلات ثيمات (مقيس). المشكلة قاعدة استعمال لا لون |
| ٤ | التابات بيضا مش برايمري | ✅ **مؤكَّدة** | `rgba(255,255,255,.82)` — والوثيقة قرّرت تسيبها حبرًا غامقًا **عن قصد** لأن البرايمري لون الفعل |
| ٥ | منسدلة أصلية ومرسومة | ✅ **مؤكَّدة** | **٣٣ أصلية · ١١٤ مرسومة · ٧ صفحات فيها الاتنين** |
| ٦ | الاستِبرز غير موحّدة | ✅ **مؤكَّدة وأسوأ** | **خمس تنفيذات** (٣.١) — مش مسألة أركان |
| ٧ | ركن الجدول الداخلي مقطوع | ✅ **مؤكَّدة بصورة** | `.glass` ٢٢px `overflow:hidden` `padding:0` والجدول جوّه **٠px**. الصح = ٢٢ − ٠ = **٢٢** |
| ٨ | أرقام الجداول | ⚠️ **مؤكَّدة جزئيًا** | المحاذاة **سليمة**: ٠ من ١٬٣٦٨ خلية بلا `tabular-nums`. اللي غلط فعلًا: **رقمان في خلية واحدة بمقاسين مختلفين وبينهم مسافة نصّية لا `gap`** (`.chb-v`) |
| ٩ | شارت الميزانية | ✅ اتعمل | اترسم بالداتا الحقيقية في الجولة اللي فاتت |
| ١٠ | القطع في «قراءة سريعة» بالجهات | ✅ **مؤكَّدة بصورة** | `.glass` ٢٢px قاصّ · `padding-top:0` · `.qr-head` ركنه **٠** وملاصق للحافة — فالهوفر بيطلع مقطوعًا |
| ١١ | حشو التابات والدواير | ✅ مؤكَّدة | ضمن انحراف الأدوار |

**تسعة مؤكَّدة · واحدة مردودة · واحدة بصرية.**

### ⚠️ ملاحظة مضادّة مهمة

الأداة علّمت «قطعة وسط الشريط مدوّرة» في `.fsegs`. **فحصتها بصورة
وطلعت سليمة**: الشرائح دي بينها `gap: 6` — يعني هي **حبّات منفصلة**
لا شريط مجزّأ، والتدوير الكامل صح فيها. اتشالت من التقرير.

الفحص ده هو السبب في وجود خطوة «تحقّق قبل ما تكتب» في السكيل: تقرير
فيه إنذار كاذب واحد بيتقفل ومحدش بيقراه.

---

## ٥ · الموجات — مرتّبة بالانحراف المُزال لكل وحدة شغل

### الموجة ١ — الأركان المتداخلة والقصّ 🔴 (يوم)

خمس مواضع + اتنين قصّ. **أعلى أثر بصري لأقل شغل** — دي اللي العميل
شافها بعينه، وحلّها قاعدة واحدة في CSS.

`glass > tblwrap` · `glass > qr-head` · `glass > qr-peek` ·
`chatlist > cl-head` · `chatlist > cl-foot`

### الموجة ٢ — التحكّمات (يومان)

توحيد الزرار والتاب والوسم والشريحة والحقل على ثلاث ارتفاعات، وشيل
نسخة `.fsegs` المكتوبة بالإيد في الميزانية، وتصحيح `.vtog` لمربع.
**بتشيل نص الانحراف.**

### الموجة ٣ — الاستِبر (يومان)

كمبوننت `<Steps>` واحد يستبدل الخمسة.

### الموجة ٤ — المنسدلات (يوم)

قرار لكل موضع من الـ٧ صفحات المختلطة: أصلية ولا مرسومة.

### الموجة ٥ — التوكنز (يوم)

٧٩ لونًا حرفيًا → توكن · ٧٠ نصف قطر خارج السلّم → سلّم ·
`.sub` من ١٥ مقاس لواحد.

**الإجمالي: ٧ أيام عمل تقريبًا للوصول لانحراف صفر.**

---

## ٦ · بعد كل موجة

```bash
npm run build && node tools/uiaudit.mjs --themes
```

ورقم الانحراف يتسجّل في `docs/UI_STANDARDS.md` § ٤. لو زاد، الموجة
مرفوضة.

---

## ٧ · نتيجة الموجة ١ — ١٠ سبتمبر ٢٠٢٦

**نقطة الحفظ قبل التنفيذ:** الوسم `v1.1-pre-wave1` على `357576a`
(بيشمل الوثيقة والتقرير — بعكس `v1.0-pre-ui-audit` اللي واقف على
`ddf4467` قبلهم)، ونسخة ملفات `abanumay-app_backup_20260910-1607.tar.gz`.

### اللي اتنفّذ

| الموضع | الحكم | التنفيذ |
|---|---|---|
| `glass > tblwrap` | 🔴 حقيقي | كارت الجدول بقى `.tblcard` بحشو `7px`، والبلاطة المرسومة (`td`) بتدوّر أركانها بـ`--slab` |
| `glass > qr-head` | 🔴 حقيقي | الشريط أخد حشو `7px` والهوفر بقى حبّة جوّاه بركن `--r2` بدل ما يتبتر على المنحنى |
| `glass > qr-peek` | ⚪ إنذار كاذب | شفّاف تمامًا — نصف قطره ما كانش بيبان |
| `chatlist > cl-head` | ⚪ إنذار كاذب | شفّاف تمامًا |
| `chatlist > cl-foot` | ⚪ إنذار كاذب | ظلّه `inset` من فوق بس، ملوش رسم عند ركن التحت |

### اللي القياس كشفه وما كانش في القايمة 🔴

**تلات معاملات مختلفة لنفس الكمبوننت — «جدول جوّه كارت»:**

| الشاشات | الحشو | القصّ |
|---|---|---|
| `/projects` · `/entities` | ٦٫٤px | `visible` |
| `/budget` · `/reports/screen/*` | **٠** | **`hidden`** |
| `/projects/20940` | ٢٣٫٢px | `visible` |

وكلها مكتوبة `style` سطرية في ستّ مواضع في الـTSX — يعني خارج نظام
التوكنز أصلًا. اتوحّدوا في كلاس واحد `.tblcard`، والـ`style` السطرية
اتشالت من الستّة.

### تصحيحات في الأدوات

- **`uiaudit.mjs` · الركن المتداخل:** بقى يشترط إن الابن بيرسم فعلًا
  عند الركن (خلفية · صورة · ظلّ خارجي · حدّ · حاوية بتقصّ). الشرط ده
  هو اللي فرز التلاتة الكاذبة من الخمسة.
- **`uiaudit.mjs` · الحاوية القاصّة:** الحاوية اللي بتتزحلق أفقيًا بس
  المتصفح بيحوّل محورها الرأسي لـ`auto` تلقائيًا، فكانت بتتعلّم غلط.
  الفحص بقى على القصّ الرأسي الحقيقي.
- **`contrast.mjs` · أخذ العيّنة:** الشريط اللي بيقيس الخلفية بقى
  يفضل جوّه صندوق العنصر لو خلفيته صمّاء. الزرار الصغير نصّه بيملا
  صندوقه، فشريط ٤px فوقه كان بيقع على حافته ويخلط اللونين.

### الرقم

| | قبل | بعد |
|---|---|---|
| انحراف الأدوار | ٤٥ | **٤٤** |
| ركن متداخل مكسور | ٥ | **٠** |
| حاوية قاصّة بلا حشو | ٢ | **٠** |
| تباين تحت AA | ٠ | **٠** ✅ |

### حالة مفتوحة 🔴

`.aishut-go` — دعوة «حلّل المشروع». الخلفية المحسوبة `#144547`
صمّاء، والمرسوم فعليًا تدرّج فاتح (`99,136,133 → 124,156,151`)،
يعني **تحت AA في التلات ثيمات**. استبعدنا بالقياس: `.glass::before`
و`backdrop-filter` على `.aicard` و`.grain`/`.grd` وأي `opacity`
أو `filter` أو `mix-blend-mode` على كل الآباء. السبب لسه مجهول
والحالة راحت للموجة ٢.

---

## ٨ · نتيجة الموجة ٢ — التحكّمات

### السبب الجذري اللي ما كانش في القايمة

**الوثيقة أعلنت سلّم ارتفاعات وسلّم خط، والستايل ما كانش يعرفهم.**
`--h-sm` و`--h-md` و`--fs-1` … ولا واحد منهم كان موجودًا في
`:root`. فكل شاشة كانت بتوصل لارتفاعها بالحشو والخط بالصدفة: ٣٣
هنا · ٣٠ هناك · ٣١ في التالتة. السلّم اتكتب في CSS، والارتفاع بقى
`min-height` صريح من التوكن والحشو الأفقي بس هو اللي بيتظبط.

### اللي اتصلّح

| الدور | كان | بقى |
|---|---|---|
| الزرار | ٣٣px · خط ١٣٫٢٨ · حشو ١٨ | `--h-md` ٣٤ · `--fs-3` · ١٦ |
| الزرار الصغير | **معرَّف مرتين** (٢٨/٢٩) | `--h-sm` ٢٨ · `--fs-2` · ١٢ — تعريف واحد |
| الوسم | ٤ ارتفاعات (١٥·١٨·١٩·٢٠) | `--h-xs` ١٨ · `--fs-1` · ٨ |
| شريحة الحالة | ٣١ و٣٠ | `--h-sm` ٢٨ |
| شريحة الأدوات | ٣٠ (خارج السلّم) | `--h-sm` ٢٨ |
| الحقل | كبسولة ٩٩٩ / مستطيل ١٠ · ٢٨ و٢٩ | مستطيل `--r2` · `--h-sm` |
| `.vtog` | **٣٢×٣٠** | ٣٠×٣٠ (ملاحظة العميل رقم ١) |
| ترويسة الجدول | حشو ١١ · خط ١١٫٨٤ | ٤٤px · حشو ١٢ · `--fs-1` |
| خلية الجدول | حشو ١٤ | ٤٨px · حشو ١٢ · `--fs-3` |
| `.sub` | **١٥ مقاسًا** | `--fs-1` واحد + `.mut` للّون وحده |
| `.fsegs` في الميزانية | ماركب مكتوب بالإيد | كمبوننت `Segments` |

الترويسة والخلية بقوا بنفس الحشو الأفقي — قبل كده كانوا ١١ و١٤،
يعني **عنوان العمود مزحلق ٣px عن أرقامه**. دي نص ملاحظتك رقم ٨.

### 🔴 الاكتشاف الأكبر — دعوة أساسية بتترسم باهتة

`.aishut-go` («حلّل المشروع») خلفيتها المحسوبة `#144547` صمّاء،
لكن البكسل المرسوم كان تدرّجًا فاتحًا: **٣٫٩:١ بدل ١١:١**، تحت AA
على زرار شغّال، في التلات ثيمات.

السبب طلع **مش في الزرار**: تدرّج رصيف القرار (`.decdock::before`)
ارتفاعه ٢١٠px، والمساحة المحجوزة تحت الشاشة ١١٢–١٥٢px، والعمود
اللازق بيحجز ارتفاع الشريط بس (~٦٢px). فآخر ١٤٨px من الكارت —
وفيهم الدعوة — كانوا قاعدين **جوّه التدرّج**.

ده النوع اللي المراجعة بالعين ما بتمسكهوش: اللون المحسوب سليم
تمامًا، والفرق في البكسل المرسوم بس. مسكه قياس البكسل.

**الحلّ:** `--dock-h` توكن واحد بيحكم التلاتة. التدرّج وظيفته يذوّب
اللي بيعدّي تحته وهو ماشي، لا اللي واقف مستقرّ.

### تباين الحبّات

| الموضع | كان | السبب | الحلّ |
|---|---|---|---|
| `.tag` على بطاقة الجهة | ٢٫٩٩:١ | أرضية `.66` بتورّث السطح | `.92` — الحبّة تبني أرضيتها |
| `.tag.mute` | ٢٫٩٩:١ | `background` مختصر بيمسح الأرضية | `background-image` زي إخواتها |
| `.tag.ret` غامق | ٣٫٨١:١ | حبر الزرار على أرضية الحبّة | `--ret-ink3` |
| `.tag.ok` غامق | ٤٫٥:١ | حدّي | `--ok-ink3` |
| `.tag.no` غامق | ٤٫١٨:١ | حدّي | `--no-ink3` أفتح |
| `.qr-count` | ٤٫٤١:١ | حبر `--t2` على أرضية ٧٪ | `--t1` |

### تصحيحات في `contrast.mjs`

الفاحص كان بيدّي إنذارات كاذبة كتير، وفاحص فيه إنذار كاذب بيتقفل
ومحدش بيقراه. اتغيّر من «شريط أعمى فوق النصّ وتحته» لـ**ترتيب
مصادر**:

1. الفراغ الرأسي **بين سطور النصّ وجوّه صندوق العنصر** — دقيق
2. لو العنصر قصير (الحبّة ١٨px ونصّها ١١٫٢): الفراغ **الجانبي**،
   وبشرط إن العنصر له حشو أفقي معلَن ≥ ٥px — يعني الفراغ ده أرضيته
   هو لا عنصر جنبه
3. لو النصّ مالي صندوقه وملوش حشو: الخلفية **تتحسب من سلسلة
   الآباء** بتركيب ألوان الخلفية لحد أول لون صمّاء

النتيجة: **٢٨ → ٠** من غير ما نخفّض الحدّ.

### أداة تالتة — `tools/uicheck.mjs`

`uiaudit` بيمسك الدرِفت و`contrast` بيمسك القراءة. مفيش حاجة كانت
بتمسك **الكسر**: خطأ كونسول · نصّ اتقصّ بلا نقط · عنصر خرج عن
المنظر · تمرير أفقي على الصفحة · أرقام عربية-هندية. الأداة دي
بتمشي على ٢٣ مسارًا × ٣ ثيمات وبتحفظ لقطة لكل واحد.

أول تشغيلة مسكت: ترويسات `LiveReport` من غير غلاف `.th-t` — «مدة
التنفيذ الفعلي» كانت بتتقطع في نص الكلمة بلا نقط، لأن الجدول ده
مكتوب مرة تانية بدل ما يستعمل `DataTable`.

### الأرقام

| | قبل الجرد | بعد ١ | **بعد ٢** |
|---|---|---|---|
| انحراف الأدوار | ٤٥ | ٤٤ | **٠** |
| نصوص تحت AA (٦٩ صفحة) | — | ١٧ | **٠** |
| صفحات فيها كسر (٦٩) | — | — | **٠** |

**التقسيم الصادق للـ٤٤:** ٢٩ اتحلّوا في الستايل، و١٥ كانوا قياسًا
غلطًا في الأداة (الزرار بمقاسين دور واحد · ارتفاع الخلية بيتحدّد
بالمحتوى · أعمدة التحديد · سلّم الوسم · الحبّات المفصولة بـgap).
