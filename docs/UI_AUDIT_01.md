# جرد الواجهة رقم ١

**التاريخ:** ١٠ سبتمبر ٢٠٢٦ · **النسخة المقيسة:** `v1.0-pre-ui-audit`
**النطاق:** ٢٣ مسارًا × ٣ ثيمات = ٦٩ قياس صفحة · ملف ستايل ٣٬٧٣٢ سطرًا
**الأداة:** `tools/uiaudit.mjs` + `tools/uiaudit-css.mjs` — يتشغّلوا تاني في أي وقت

---

## ١ · السبب

نظام الألوان موكَّن بالكامل؛ **الشكل والمقاس والحشو لأ**. كل شاشة جديدة
كانت بتكتب أرقامها بإيدها لأن مفيش سلّم مفروض يمنعها. فالاختلاف مش نتيجة
قرار غلط — هو **النتيجة الحتمية** لغياب مانع. وده معناه إن الإصلاح
بالترقيع بيرجع بعد شهر؛ اللي بيمنع الرجوع هو السلّم + الفحص الآلي.

---

## ٢ · الأرقام

| القياس | الآن | الهدف |
|---|---|---|
| **انحراف الأدوار وقت التشغيل** | **٤٥** | ٠ |
| مقاسات خط في الستايل | ٨٥ | ٧ |
| قيم حشو | ١٥٩ | ٩ |
| قيم `gap` | ٦٢ | ٩ |
| نصف قطر حرفي | ١٠٩ (٧٠ خارج السلّم) | ٠ |
| ألوان حرفية `#hex` | ١٥٣ استعمال · ٧٩ قيمة | ٠ |
| صناديق مستطيلة يفترض إنها مربّعة | ٣ | ٠ |
| ركن متداخل مكسور | ٥ | ٠ |
| حاوية قاصّة بلا حشو | ٢ | ٠ |
| صفحات بنمطَي منسدلة | ٧ | ٠ |
| خلايا رقمية بلا `tabular-nums` | **٠ من ١٬٣٦٨** | ٠ ✅ |
| أكتر من دعوة أساسية في شاشة | **٠** | ٠ ✅ |

---

## ٣ · اللي الأداة لقته والعين ما شافتهوش

### ٣.١ خمس تنفيذات مختلفة للاستِبر 🔴

مش «مرة كيرفي ومرة شارب» — **خمسة كمبوننتس منفصلة بتعمل نفس الحاجة**:

| التنفيذ | المكان | قطر النقطة | التخطيط |
|---|---|---|---|
| `.steps` / `.step` | أفقي عام | **١٠px** | grid أعمدة + خط واصل |
| `.stepv` (`VSteps`) | رأسي | **١٠px** | flex عمود |
| `.agr-step` / `.agr-dot` | الاتفاقية | **١٦px** | grid ٣ أعمدة |
| `.pay-step` / `.pay-dot` | الدفعات | **١٥px** | grid auto-fit |
| `.th-step` / `.th-dot` | تفكير المساعد | **٥px** | flex سطر |

**١٥px مقابل ١٦px** هي بصمة الدرِفت الكلاسيكية: رقمان اتكتبوا بالإيد
في وقتين مختلفين لنفس الشيء. **الإصلاح:** كمبوننت `<Steps>` واحد
بمحورين (أفقي/رأسي) وقطر نقطة من السلّم.

### ٣.٢ الحقل كبسولة في مكان ومستطيل في مكان 🔴

| الشاشة | نصف القطر | الارتفاع |
|---|---|---|
| `/projects` · `/entities` | **٩٩٩px** (كبسولة) | ٢٨ |
| `/budget` · `/reports` | **١٠px** (مستطيل) | ٢٩ |

نفس الدور، شكلان مختلفان تمامًا. ده أوضح من كل اللي في قايمتك ومحدش
ذكره.

### ٣.٣ شرائح الحالة اتكتبت مرتين

`.fsegs` في `/projects` و`/entities` و`/reports` جايّة من كمبوننت
`Segments` وارتفاعها **٣١px**. في `/budget` نفس الشكل **مكتوب ماركب
بالإيد** وارتفاعه **٣٠px**. البكسل مش المشكلة — المشكلة إن فيه نسختين
من نفس الكمبوننت، والتانية مش هتاخد أي تحسين يحصل في الأولى.

### ٣.٤ الوسم بأربعة ارتفاعات

١٥ · ١٨ · ١٩ · ٢٠px. الأغلبية الساحقة ١٨ (٣٠ موضع)، والباقي حالات
فردية في `/` و`/budget`.

### ٣.٥ النصّ المساعد بخمستاشر مقاس

`.sub` مقاسه بيتراوح من ١٠.٨٨ لـ١٢.٨px عبر الشاشات. دور واحد،
خمستاشر قيمة.

### ٣.٦ ألوان التوكنز مكتوبة حرفيًا كمان

`#2FC7B6` (٦ مرات) · `#144547` (٤) · `#A8D93C` (٤) · `#E8B045` (٤) —
دي **كلها توكنز موجودة**، ومكتوبة تاني بالإيد. يعني تغيير اللون في
`:root` مش هيوصلهم.

### ٣.٧ ملاحظة على الوثيقة نفسها ⚠️

سلّم الوثيقة حطّ الوسم في `--h-sm: 28px`. **القياس بيقول ٱلوثيقة غلط**:
الوسم الحقيقي ١٨px بخط ١٠.٨٨، وده الصح لجدول كثيف — ٢٨px هيبقى ضخمًا
داخل السطر. الوسم **مش عنصر تحكّم**، هو تسمية غير قابلة للضغط، فمالوش
دعوة بسلّم الأهداف اللمسية.

**التعديل المطلوب على الوثيقة:** إضافة `--h-xs: 20px` للتسميات غير
التفاعلية، والوسم ياخده.

---

## ٤ · ملاحظاتك الإحدى عشرة — الحالة بعد القياس

| # | الملاحظة | الحالة | التفصيل |
|---|---|---|---|
| ١ | أيقونات المنيو مستطيلة | ✅ **مؤكَّدة** | `.vtog button` = **٣٢×٣٠** في `/entities` |
| ٢ | اللوجو المصغّر/الممتد | ⚪ بصري | مش قابل للقياس — قرار في الوثيقة |
| ٣ | CTA مش موحّدة | ❌ **مردودة** | الخمس فاريَنتس **لونها ثابت** في التلات ثيمات (مقيس). المشكلة قاعدة استعمال لا لون |
| ٤ | التابات بيضا مش برايمري | ✅ **مؤكَّدة** | `rgba(255,255,255,.82)` — والوثيقة قرّرت تسيبها حبرًا غامقًا **عن قصد** لأن البرايمري لون الفعل |
| ٥ | منسدلة أصلية ومرسومة | ✅ **مؤكَّدة** | **٣٣ أصلية · ١١٤ مرسومة · ٧ صفحات فيها الاتنين** |
| ٦ | الاستِبرز غير موحّدة | ✅ **مؤكَّدة وأسوأ** | **خمس تنفيذات** (٣.١) — مش مسألة أركان |
| ٧ | ركن الجدول الداخلي مقطوع | ✅ **مؤكَّدة بصورة** | `.glass` ٢٢px `overflow:hidden` `padding:0` والجدول جوّه **٠px**. الصح = ٢٢ − ٠ = **٢٢** |
| ٨ | أرقام الجداول | ⚠️ **مؤكَّدة جزئيًا** | المحاذاة **سليمة**: ٠ من ١٬٣٦٨ خلية بلا `tabular-nums`. اللي غلط فعلًا: **رقمان في خلية واحدة بمقاسين مختلفين وبينهم مسافة نصّية لا `gap`** (`.chb-v`) |
| ٩ | شارت الميزانية | ✅ اتعمل | اترسم بالداتا الحقيقية في الجولة اللي فاتت |
| ١٠ | القطع في «قراءة سريعة» بالجهات | ✅ **مؤكَّدة بصورة** | `.glass` ٢٢px قاصّ · `padding-top:0` · `.qr-head` ركنه **٠** وملاصق للحافة — فالهوفر بيطلع مقطوعًا |
| ١١ | حشو التابات والدواير | ✅ مؤكَّدة | ضمن انحراف الأدوار |

**تسعة مؤكَّدة · واحدة مردودة · واحدة بصرية.**

### ⚠️ ملاحظة مضادّة مهمة

الأداة علّمت «قطعة وسط الشريط مدوّرة» في `.fsegs`. **فحصتها بصورة
وطلعت سليمة**: الشرائح دي بينها `gap: 6` — يعني هي **حبّات منفصلة**
لا شريط مجزّأ، والتدوير الكامل صح فيها. اتشالت من التقرير.

الفحص ده هو السبب في وجود خطوة «تحقّق قبل ما تكتب» في السكيل: تقرير
فيه إنذار كاذب واحد بيتقفل ومحدش بيقراه.

---

## ٥ · الموجات — مرتّبة بالانحراف المُزال لكل وحدة شغل

### الموجة ١ — الأركان المتداخلة والقصّ 🔴 (يوم)

خمس مواضع + اتنين قصّ. **أعلى أثر بصري لأقل شغل** — دي اللي العميل
شافها بعينه، وحلّها قاعدة واحدة في CSS.

`glass > tblwrap` · `glass > qr-head` · `glass > qr-peek` ·
`chatlist > cl-head` · `chatlist > cl-foot`

### الموجة ٢ — التحكّمات (يومان)

توحيد الزرار والتاب والوسم والشريحة والحقل على ثلاث ارتفاعات، وشيل
نسخة `.fsegs` المكتوبة بالإيد في الميزانية، وتصحيح `.vtog` لمربع.
**بتشيل نص الانحراف.**

### الموجة ٣ — الاستِبر (يومان)

كمبوننت `<Steps>` واحد يستبدل الخمسة.

### الموجة ٤ — المنسدلات (يوم)

قرار لكل موضع من الـ٧ صفحات المختلطة: أصلية ولا مرسومة.

### الموجة ٥ — التوكنز (يوم)

٧٩ لونًا حرفيًا → توكن · ٧٠ نصف قطر خارج السلّم → سلّم ·
`.sub` من ١٥ مقاس لواحد.

**الإجمالي: ٧ أيام عمل تقريبًا للوصول لانحراف صفر.**

---

## ٦ · بعد كل موجة

```bash
npm run build && node tools/uiaudit.mjs --themes
```

ورقم الانحراف يتسجّل في `docs/UI_STANDARDS.md` § ٤. لو زاد، الموجة
مرفوضة.
