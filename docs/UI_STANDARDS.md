# معايير الواجهة — نظام منح أبانمي

> **ليه الوثيقة دي موجودة.** الدرِفت اللي العميل شافه («نفس الكمبوننت
> شكله مختلف من مكان لمكان») مش نتيجة قرارات غلط — هو نتيجة إن مفيش
> حاجة **بتمنع** الاختلاف. نظام الألوان عندنا موكَّن بالكامل، لكن
> الشكل والمقاس والحشو كانوا بيتكتبوا بالإيد في كل موقع.
>
> فالوثيقة دي **قابلة للفحص**، لا للتذوّق: كل قاعدة فيها إما رقم من
> سلّم، أو شرط بيتقاس آليًّا بـ`tools/uiaudit.mjs`. اللي ما ينفعش
> يتقاس مش قاعدة، هو توصية ومكتوب كده.

**النسخة:** ١ · ١٠ سبتمبر ٢٠٢٦ · نقطة الحفظ `v1.0-pre-ui-audit`

---

## ٠ · من فين بنجيب «الأفضل»؟

سؤال العميل الحقيقي: «الاستِبر شارب ولا كيرفي من جوّه؟ المنيو مربع
ولا مستطيل؟» — الإجابة **مش ذوق في أغلب الحالات**. القرارات دي بتتقسم
تلاتة، وكل واحد ليه مصدر مختلف:

### أ · قواعد محسومة — هندسة أو معيار

مفيش فيها رأي. الجواب واحد وبيتحسب.

| السؤال | الجواب | المصدر |
|---|---|---|
| ركن الابن جوّه أب مدوّر | `inner = outer − padding` | الهندسة: مركزان مختلفان بيدّوا ركنًا غير متحد المركز. [Cloud Four — The Math Behind Nesting Rounded Corners](https://cloudfour.com/thinks/the-math-behind-nesting-rounded-corners/) |
| صندوق الأيقونة مربع ولا مستطيل | **مربع دايمًا** | الأيقونة نفسها مرسومة في مربع (viewBox). صندوق مستطيل بيزحزح مركزها البصري عن مركزه. |
| التباين الأدنى للنص | ٤.٥:١ (و٣:١ للنص الكبير) | WCAG 2.2 — مقيس عندنا بـ`tools/contrast.mjs` |
| أصغر هدف لمس | ٢٤×٢٤px حدًّا أدنى | WCAG 2.2 Target Size (Minimum) |
| نمط القائمة المنسدلة | `<select>` الأصلية إلا لو محتاج محتوى غنيّ | [WAI-ARIA Authoring Practices](https://www.w3.org/WAI/ARIA/apg/) — النمط المخصَّص بيلزمه تنفيذ كيبورد كامل، والأصلية بتجيبه ببلاش |

### ب · أعراف متقاربة — نبصّ على اللي اتفقوا عليه

لما تلات أنظمة كبيرة مستقلة تقول نفس الحاجة، دي مش صدفة. الأنظمة
اللي بنرجع لها ومنشورة كلها علنًا:

- **Material Design 3** (جوجل) · **Apple Human Interface Guidelines**
- **IBM Carbon** · **Atlassian Design System** · **Shopify Polaris**
- **GOV.UK Design System** (أقوى مرجع للنماذج والحقول)
- **Microsoft Fluent 2**

**طريقة الاستعمال:** ما ننقلش نظامًا بعينه — نشوف نقطة الاتفاق.
لو اتفقوا، الاتفاق هو القاعدة. لو اختلفوا، القرار بينتقل للقسم (ج).

### ج · قرارنا إحنا — يتكتب مرة ويتقفل

اللي الأنظمة بتختلف فيه (شكل التاب المختار مثلًا) قرار هوية، مش
صحّ وغلط. القاعدة الوحيدة هنا: **يتقرّر مرة، ويتكتب هنا، وما
يتناقشش تاني في كل شاشة.**

---

## ١ · السلالم

كل قيمة في الواجهة لازم تكون من سلّم. الرقم اللي برّه السلّم = خطأ،
لا «استثناء».

### نصف القطر

```
--r1: 10px   الأشياء الصغيرة: خيار في قايمة · وسم داخلي · خانة
--r2: 15px   المتوسطة: زرار كبير · حقل · شريحة
--r3: 22px   الكارت والسكشن
--r4: 28px   الحاويات الكبيرة والشرايط العايمة
--rp: 999px  الكبسولة: الوسم · شريحة الحالة · زرار الأيقونة الدائري
```

**قاعدة الركن المتداخل (مُلزِمة):** ابن ملاصق لحافة أب مدوّر بيقصّ،
نصف قطره = `نصف قطر الأب − حشو الأب`. لو الحشو صفر، نصف القطر
يورَّث كما هو.

```css
/* غلط — الجدول جوّه كارت نصف قطره 22 والركن بيطلع مقطوع */
.glass { border-radius: var(--r3); overflow: hidden }
.tblwrap { border-radius: 0 }

/* صح */
.glass { --pad: 0px; border-radius: var(--r3); overflow: hidden }
.tblwrap { border-radius: calc(var(--r3) - var(--pad)) }
```

> يُفحص آليًّا: «ركن الابن أصغر من ركن الأب».

### ارتفاعات التحكّم

تلاتة بس. أي رابع = خطأ.

```
--h-xs: 20px   تسمية غير تفاعلية: الوسم · الشارة
--h-sm: 28px   شريحة · زرار أيقونة صغير
--h-md: 34px   الافتراضي: زرار · تاب · حقل · بحث · شريحة أدوات
--h-lg: 42px   الدعوة الأساسية في شريط القرار
```

> `--h-xs` اتضاف بعد **جرد ١**: الوثيقة كانت حاطّة الوسم في `--h-sm`،
> والقياس ردّ عليها — الوسم الحقيقي ١٨px بخط ١٠.٨٨، و٢٨px بيبقى ضخمًا
> جوّه سطر في جدول كثيف. والسبب إن **الوسم مش عنصر تحكّم**: مش قابل
> للضغط، فمالوش دعوة بسلّم الأهداف اللمسية.

### صناديق الأيقونات — **مربّعة دايمًا**

```
24 · 30 · 38 · 44
```

والأيقونة جوّاها ≈ ٦٠٪ من الصندوق. الصندوق المستطيل خطأ يُفحص آليًّا.

### المسافات

```
2 · 4 · 6 · 8 · 12 · 16 · 22 · 32 · 48
```

`gap` و`padding` من السلّم ده حصرًا.

### الخط

```
--fs-1: .70rem   شارة · تسمية دقيقة
--fs-2: .76rem   نصّ مساعد (`.sub`) — **قيمة واحدة، مش خمستاشر**
--fs-3: .82rem   نصّ الجدول والقائمة
--fs-4: .90rem   عنوان كارت
--fs-5: 1.05rem  عنوان سكشن
--fs-6: 1.5rem   عنوان صفحة
--fs-7: 2.1rem   الرقم البطل
```

---

## ٢ · تشريح الكمبوننتس

### الزرار — والقاعدة اللي كانت ناقصة

الفاريَنتس الخمسة **ألوانها متسقة فعلًا** (مقيس). المشكلة كانت إن
مفيش قاعدة تقول امتى تستخدم أنهي، فنفس الإجراء كان بيتقري مختلفًا
من شاشة لشاشة.

| الفاريَنت | امتى | كام في الشاشة |
|---|---|---|
| `btn-p` | **الفعل الأساسي** — اللي الشاشة موجودة عشانه | **واحد بالظبط، أو صفر** |
| `btn-1` | فعل ثانوي بارز | صفر أو أكتر |
| `btn-2` | فعل عادي · إلغاء · رجوع | صفر أو أكتر |
| `btn-3` | فعل معلوماتي (أزرق) — بيفتح تفاصيل ما بيغيّرش حاجة | صفر أو أكتر |
| `btn-d` | فعل هدّام — حذف · رفض · إيقاف | صفر أو أكتر |

**قاعدة مُلزِمة:** شاشة فيها أكتر من `btn-p` واحد = خطأ. الفعل
الأساسي واحد، وغير كده المستخدم ما بيعرفش الشاشة عايزة منه إيه.

### التاب

**القرار (قسم ج):** التاب المختار **حبر غامق على أرضية زجاج فاتحة**،
مش أرضية بالبرايمري.

**السبب:** البرايمري عندنا (`--solid`) هو **لون الفعل** — الزرار
الأساسي والشريط اللازق. لو التاب المختار أخد نفس اللون، بيبقى فيه
لونان بيقولا «اضغط هنا» في نفس الشاشة، والتاب مش فعل — هو **موضع**.
والسطر السفلي (`::after` بالبرايمري) كافي للدلالة، وده اللي Material
وCarbon وPolaris متفقين عليه.

- الارتفاع `--h-md` · نصف القطر `--r2` · الحشو الأفقي `16`
- المختار: أرضية زجاج + حبر `--t1` + سطر سفلي ٢px بالبرايمري
- غير المختار: بلا أرضية + حبر `--t2`

### القائمة المنسدلة — نمط **واحد**

المقيس النهارده: **٣٣ `<select>` أصلية · ١١٤ مرسومة · ٧ صفحات فيها
النمطان مع بعض**.

**القاعدة:**

- خيارات نصّية بسيطة (سنة · حالة · قسم) ← **`<select>` أصلية** بلبس
  `.fsel`. الكيبورد والموبايل والقارئ الشاشة بيجوا ببلاش.
- محتاج **محتوى غنيّ** جوّه الخيار (عدّاد · أيقونة · سطر شرح · اختيار
  متعدد · بحث داخلي) ← **النمط المرسوم** `.fmenu` بـ`aria-haspopup`
  وتنفيذ كيبورد كامل.
- **ممنوع** النمطان في نفس المجموعة من الأدوات. لو الفرز مرسوم،
  الفلتر اللي جنبه يبقى مرسومًا.

### الوسم

كبسولة `--rp` · ارتفاع `--h-xs` · خط `--fs-1` · حشو أفقي `10`.
**ارتفاع واحد.** المقيس كان أربعة (١٥ · ١٨ · ١٩ · ٢٠).

### الجدول

- الحاوية `--r3` وبتقصّ ← الجدول جوّاها ياخد نفس الركن (قاعدة التداخل)
- ترويسة: ارتفاع `44` · خط `--fs-1` · حشو أفقي `14`
- خلية: ارتفاع `48` · خط `--fs-3` · حشو أفقي `14`
- **الأرقام:** `font-variant-numeric: tabular-nums` + محاذاة للنهاية.
  رقمان في خلية واحدة بينهم `gap: 6` — مش مسافة نصّية.
- المحتوى اللي أطول من العمود **يتقصّ**، ما ينزلش سطرًا

### الاستِبر — سؤال «شارب ولا كيرفي»

**أول حاجة، الفرق بين شكلين الناس بتخلط بينهم:**

- **شريط مجزّأ** (`gap: 0`) — القطع ملتصقة، والشكل بيقول «دول واحد
  متقسّم». القاعدة تحت بتخصّه.
- **حبّات منفصلة** (`gap > 0`) — كل واحدة كيان قائم بذاته، فالتدوير
  الكامل صح فيها. `.fsegs` عندنا من دول (`gap: 6`)، وده **سليم**.

**والجواب للشريط المجزّأ من قاعدة التداخل، مش من الذوق:**

القطعة اللي **بتلمس حافة الحاوية** بتاخد ركن الحاوية على الجهة دي؛
والجهة اللي **بتلمس قطعة أختها** بتبقى قائمة (صفر). يعني:

```
[ أول قطعة: مدوّرة من برّه، قائمة من جوّه ]
[ قطعة وسط: قائمة من الجهتين                ]
[ آخر قطعة: قائمة من جوّه، مدوّرة من برّه   ]
```

القطعة اللي مدوّرة من الجهتين **وسط شريط ملتصق** بتقول «أنا منفصلة» —
وهي مش منفصلة، هي خطوة في تسلسل. الشكل لازم يقول التسلسل.

**وتنفيذ واحد لا خمسة.** جرد ١ لقى خمس تنفيذات مستقلة لنفس الفكرة
(`.steps` · `.stepv` · `.agr-step` · `.pay-step` · `.th-step`) بأقطار
نقاط ٥ · ١٠ · ١٥ · ١٦px. المطلوب كمبوننت `<Steps>` واحد بمحورين
وقطر من السلّم.

### الشعار

- **المنيو مطويّة** ← العلامة وحدها، في صندوق مربع `38`
- **المنيو مفرودة** ← القفل الأفقي (العلامة + اسم المؤسسة)، محاذاة
  لبداية السطر (اليمين في RTL)
- الانتقال بين الحالتين تبديل مكوّن، مش تكبير نفس الصورة

---

## ٣ · القواعد الملزِمة (تُفحص آليًّا)

| # | القاعدة | الفحص |
|---|---|---|
| ١ | كل نصف قطر من السلّم | `uiaudit-css` — خارج السلّم |
| ٢ | ركن الابن = ركن الأب − الحشو | `uiaudit` — الركن المتداخل |
| ٣ | صندوق الأيقونة مربع | `uiaudit` — المربّعات |
| ٤ | ارتفاع واحد لكل دور | `uiaudit` — انحراف الأدوار |
| ٥ | `btn-p` واحد بالحد الأقصى في الشاشة | `uiaudit` — عدّاد الفاريَنت |
| ٦ | نمط منسدلة واحد لكل مجموعة أدوات | `uiaudit` — نمط القوائم |
| ٧ | حاوية بتقصّ ليها حشو ≥ ٤px | `uiaudit` — الحاويات القاصّة |
| ٨ | التباين ≥ ٤.٥ في التلات ثيمات | `tools/contrast.mjs` |
| ٩ | كل الأرقام لاتينية | مراجعة نصّية |
| ١٠ | مفيش لون حرفي برّه `:root` | `uiaudit-css` — الألوان |

---

## ٤ · حالة الالتزام — أول قياس

مقيس على ٢٣ مسارًا × ٣ ثيمات، `v1.0-pre-ui-audit`:

| | القيمة | الهدف |
|---|---|---|
| انحراف الأدوار وقت التشغيل | **٤٥** | ٠ |
| مقاسات الخط | **٨٥** | ٧ |
| قيم الحشو | **١٥٩** | ٩ |
| قيم `gap` | **٦٢** | ٩ |
| نصف قطر حرفي | **١٠٩** (٧٠ خارج السلّم) | ٠ |
| ألوان حرفية | **٧٩ قيمة** | ٠ |
| صناديق مستطيلة | **٣** | ٠ |
| ركن متداخل مكسور | **٥** | ٠ |
| حاوية قاصّة بلا حشو | **٢** | ٠ |
| صفحات بنمطَي منسدلة | **٧** | ٠ |

| خلايا رقمية بلا `tabular-nums` | ٠ من ١٬٣٦٨ | ٠ ✅ |
| أكتر من دعوة أساسية في شاشة | ٠ | ٠ ✅ |

الأرقام دي تتعاد بعد كل موجة، والوثيقة دي تتحدّث بالقيمة الجديدة.

**سجل الجرد:** [`UI_AUDIT_01.md`](./UI_AUDIT_01.md) — ١٠ سبتمبر ٢٠٢٦.

---

## ٥ · إزاي تضيف كمبوننت جديدًا

1. **دوّر على دور موجود** قبل ما تخترع واحدًا. زرار جديد = فاريَنت
   من الخمسة، مش سادس.
2. **كل قيمة من سلّم.** لو محتاج قيمة مش في السلّم، السؤال يبقى
   «هل السلّم ناقص؟» لا «هأكتب الرقم بالإيد».
3. **شغّل الجرد قبل الكوميت:**
   ```bash
   npm run build && node tools/uiaudit.mjs --themes
   ```
   لو الانحراف زاد، الكمبوننت مش جاهز.

---

## ملاحق

- `tools/uiaudit.mjs` — الجرد وقت التشغيل
- `tools/uiaudit-css.mjs` — الجرد الثابت على ملف الستايل
- `tools/contrast.mjs` — فحص التباين

**المصادر:**
[Cloud Four — The Math Behind Nesting Rounded Corners](https://cloudfour.com/thinks/the-math-behind-nesting-rounded-corners/) ·
[W3C WAI-ARIA Authoring Practices](https://www.w3.org/WAI/ARIA/apg/) ·
[Material Design 3](https://m3.material.io/) ·
[IBM Carbon](https://carbondesignsystem.com/) ·
[GOV.UK Design System](https://design-system.service.gov.uk/) ·
[Shopify Polaris](https://polaris.shopify.com/) ·
[Apple HIG](https://developer.apple.com/design/human-interface-guidelines/)
