# أدوات الفحص · إزاي تشتغل بسرعة

## الأمران اللي هتستعملهم

```bash
npm run check          # ١٨ ثانية · بلا متصفّح · بعد كل تعديل
npm run sweep          # ٦٠ ثانية · ثيم فاتح · على المسارات كلها
npm run sweep -- --routes /budget   # ثانيتين · المسارات المتأثّرة وحدها
npm run check:all      # ٣ دقايق · بِناء + كل شيء × المظهرين · قبل الكوميت
```

---

## ليه الشغل كان بياخد ساعة

القياس قبل أي تغيير:

| الأداة | الوقت | السبب |
|---|---|---|
| `contrast --all` | **~٢٠ دقيقة** | `waitForTimeout(2500)` × ٢٣٧ زيارة، وسكرين شوت لكل واحدة |
| `uicheck --themes` | **~٥٫٥ دقايق** | `waitForTimeout(1100)` × ٢٣٧ |
| `linkaudit` | **~٥ دقايق** | `networkidle` × ٧٩ |

و`uicheck` في ثيم واحد كانت **١م٥٠ث**، و`user time` فيها **٥٫٦ث** ·
يعني **٩٥٪ من الوقت استنّى، لا حساب**.

⚠️ **وفي ٢١ أداة بتفتح متصفّحًا**، كل واحدة بتشغّل سيرفرها
وكروميوم بتاعها **وبتلفّ على نفس الصفحات من أولها**. فالجولة
الواحدة كانت بتزور نفس الـ٢٣٧ صفحة تلات مرات.

---

## التلات تغييرات

**١ · زيارة واحدة، كل الفحوص عليها.** الفحص بقى «بروب» بيشتغل على
صفحة محمَّلة، مش برنامجًا بيمشي على المسارات بنفسه · فتمن التحميل
بيتدفع مرة لا تلاتة.

**٢ · مسارات متوازية.** طابور شغل وستّ صفحات بتسحب منه.

**٣ · التجميد بدل الاستنّى.** وده اللي أخد تلات محاولات:

- **محاولة ١** · خطوط جاهزة + إطارين. شغّلت فاحص التباين مرتين على
  **نفس البِناء** فطلع «١ ملاحظة» و«٧ ملاحظات» · أداة بترجّع
  إجابتين لنفس السؤال.
- **محاولة ٢** · نستنّى الحركات تخلص، ونصوّر لحد ما الصورة تثبت.
  بقت **أبطأ من القديم**. والقياس قال ليه:

  ```
  /projects   anims: 5   كلها infinite   shots-to-stable: 13
  /           anims: 6   كلها infinite   shots-to-stable: 13
  ```

  رصيف «اسأل أبانمي» فيه أربع حركات **لا نهائية** (`aipulse` ·
  `aisheen` · `aiglow` · `aicaret`) في كل صفحة تقريبًا · فالحركات
  ما بتخلصش، والصورة ما بتثبتش.

- **محاولة ٣ · التجميد.** الحركة المنتهية بتتودّى لآخرها (وده اللي
  المستخدم بيشوفه مستقرًّا)، واللانهائية بتتوقّف عند طور ثابت.

⚠️ **والأهمّ إن ده كان بيفسد القياس القديم كمان.** التوهّج النابض
بيغيّر بكسلات الخلفية كل إطار، والـ٢٥٠٠ مللي ما كانتش بتثبّته —
كانت بتقع على **طور عشوائي** منه. يعني «٤٫٣٥ تحت الحدّ» كان ممكن
يبقى طور حركة لا لونًا.

**والنتيجة: ٣٠ دقيقة ← ٣ دقايق، وبقت تتكرّر.** تلات تشغيلات
متتالية على نفس البِناء رجّعوا نفس الإجابة بالحرف.

---

## ⚠️ وحاجة اتكشفت في الطريق

الأدوات القديمة كانت بتقيس **بعرضين مختلفين ومحدش واخد باله**:
`contrast` بـ**1440** و`uicheck` بـ**1600**. يعني اللي بيتقصّ عند
1440 كان بيعدّي، لأن فاحص القصّ بيبصّ من شاشة أوسع.

أول ما العرض بقى واحد (**1440**، الأضيق لأنه الأمين)، طلع قصّ
حقيقي في قايمة المشاريع: وسم «في التشغيل» ٧٥px في عمود ٧٢px.
عدّى شهورًا.

---

## إزاي تضيف فحصًا جديدًا

في `tools/probes.mjs`:

```js
export const myProbe = {
  name: 'اسمه',
  shot: true,              // لو محتاج سكرين شوت · وإلا ما بيتصوّرش أصلًا
  async run(page, ctx) {   // ctx = { route, theme, shot, errs }
    const hits = await page.evaluate(() => /* فحصك */)
    return hits.map((h) => ({ say: h }))
  },
}
```

وضيفه في `tools/sweep.mjs` · وخلاص، بيشتغل على كل الصفحات بلا أي
كود تنقّل.

**ما تكتبش أداة بتلفّ على `ROUTES` بنفسها تاني** · دي كانت المشكلة.

---

## ⚠️ المسار اللي فيه مسافة · عطل كان مستخفي

أوّل مرة شغّلت الأدوات دي على جهازك طاحت:

```
Error: ENOENT: no such file or directory, open
'/Users/mohabsmacpro/Desktop/Claude%20Work%20Projects/.../src/styles/index.css'
    at tools/audit.mjs:26
```

الـ`%20` هو الدليل. `new URL(x, import.meta.url).pathname` بيرجّع
المسار **مكوَّدًا** · و`fs` ما بيفكّش الترميز. مجلّدك اسمه «Claude
Work Projects» فيه مسافتان، والمسار عندي في السحابة
`/home/claude/abanumay/app` مفيش فيه ولا مسافة · فالتكويد ما كانش
بيغيّر حرفًا واحدًا والعطل عدّى على ٢٨ أداة من غير ما يبان.

**الصحّ:**

```js
import { fileURLToPath } from 'node:url'
const ROOT = fileURLToPath(new URL('../dist/', import.meta.url))
```

(تمرير كائن `URL` مباشرة لـ`fs.readFileSync` سليم كمان · نود بيفكّ
الترميز بنفسه. اللي بيكسر هو `.pathname` وحده.)

**والدرس اتحوّل لفاحص لا لتعليق:** `tools/pathsafe.mjs` في بوّابة
الكوميت بيرفض أي `.pathname` على `import.meta.url` · لأن **بيئة
واحدة مش كفاية للتأكّد**، والأداة اللي بتعدّي عندي وبتطيح عندك
أسوأ من أداة ساكتة. والتأكّد اتعمل بنسخ المشروع لمجلّد فيه مسافة
وتشغيل الجولة كلها منه · ٧٩ زيارة، صفر ملاحظة.

---

## حزام الرصيف السفلي · بقعة عمياء معلَنة

`.decdock::before` طبقة **مقصودة** بتغيّب المحتوى تدريجيًّا وهو
داخل تحت الشريط السفلي · التعليق عندها في الـCSS بيشرح ليه. فأي
نصّ واقع جوّه الحزام **عند أول موضع تمرير** بيتقاس مغيَّبًا لا
ملوَّنًا: زرار «التالي» الأساسي طلع «١٫٠٥» ولونه سليم تمامًا،
والمستخدم بيمرّر سطرين فيطلع فوق الطبقة عادي.

فـ`contrastProbe` بيستثني الحزام ده (آخر ~١٦٦px فوق قاع الرصيف)،
**ما عدا نصّ الرصيف نفسه** · هو فوق الطبقة لا تحتها.

⚠️ **والاستثناء ده بقعة عمياء حقيقية ومعلَنة**: لون غلط فعلًا في
الحزام مش هيتمسك. مكتوب هنا عشان اللي بعدنا يعرفها، لا عشان
تتنسى · ودي أحسن من أداة بتبلّغ عن حاجة مش موجودة (نفس درس
`linkaudit`).
